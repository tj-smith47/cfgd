apiVersion: pkg.crossplane.io/v1beta1
kind: Function
metadata:
  name: function-cfgd
spec:
  # Override with your registry: kubectl set image ... or edit this field.
  # The xpkg must be built via: crossplane xpkg build --package-root=function-cfgd/package --embed-runtime-image=<runtime>
  package: registry.jarvispro.io/function-cfgd:latest
