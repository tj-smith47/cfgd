apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: machineconfigs.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: MachineConfig
    plural: machineconfigs
    shortNames:
    - mc
    singular: machineconfig
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.hostname
      name: Hostname
      type: string
    - jsonPath: .spec.profile
      name: Profile
      type: string
    - jsonPath: .status.conditions[?(@.type=="Reconciled")].status
      name: Reconciled
      type: string
    - jsonPath: .status.conditions[?(@.type=="DriftDetected")].status
      name: Drift
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for MachineConfigSpec via `CustomResource`
        properties:
          spec:
            properties:
              files:
                default: []
                description: |-
                  Files to write on the machine, either inline or fetched from a source
                  path.
                items:
                  description: A file the operator writes on a managed machine.
                  properties:
                    content:
                      description: Literal file body, written as-is. Mutually exclusive with `source`.
                      nullable: true
                      type: string
                    mode:
                      default: '0644'
                      description: 'Octal permission bits applied after the write. Default: `0644`.'
                      type: string
                    path:
                      description: |-
                        Destination path on the machine. A leading `~` expands to the home
                        directory of the user the agent runs as.
                      type: string
                    source:
                      description: |-
                        Path the body is read from instead of `content`, resolved against the
                        machine's config directory.
                      nullable: true
                      type: string
                  required:
                  - path
                  type: object
                  x-kubernetes-validations:
                  - message: each file must have content or source
                    rule: has(self.content) || has(self.source)
                type: array
                x-kubernetes-list-map-keys:
                - path
                x-kubernetes-list-type: map
              hostname:
                description: |-
                  Hostname of the machine this document describes. The agent reconciles
                  only the MachineConfig whose hostname matches its own.
                type: string
              moduleRefs:
                default: []
                description: |-
                  Modules to install on the machine, each naming a cluster-scoped
                  `Module` resource.
                items:
                  description: Reference to a module that should be installed on the machine.
                  properties:
                    name:
                      description: Name of the cluster-scoped `Module` resource to install.
                      type: string
                    required:
                      default: false
                      description: |-
                        Whether a failure to resolve or install this module fails the whole
                        reconcile. Default: `false`, so a missing module is reported and the
                        rest of the machine still converges.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              packages:
                default: []
                description: |-
                  Packages to install on top of whatever the profile declares, each
                  optionally version-pinned.
                items:
                  description: Reference to a package with optional version pin.
                  properties:
                    name:
                      description: Package name as the machine's own package manager knows it.
                      type: string
                    version:
                      description: |-
                        Exact version to pin to. Omitted, whatever the manager currently offers
                        is installed and left alone once present.
                      nullable: true
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              profile:
                description: |-
                  Name of the profile the machine reconciles against. Resolved on the
                  machine, from its own config directory or from a subscribed source.
                type: string
              systemSettings:
                additionalProperties: true
                default: {}
                description: |-
                  System configurator settings to apply, keyed by
                  `<configurator>.<setting>` (e.g. `sysctl.net.ipv4.ip_forward`). Empty,
                  no system settings are reconciled.
                type: object
            required:
            - hostname
            - profile
            type: object
            x-kubernetes-validations:
            - message: hostname must not be empty
              rule: self.hostname.size() > 0
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastReconciled:
                nullable: true
                type: string
              observedGeneration:
                format: int64
                nullable: true
                type: integer
              packageVersions:
                additionalProperties:
                  type: string
                default: {}
                description: 'Reported installed versions keyed by package name (e.g. {"kubectl": "1.28.3"}).'
                type: object
            type: object
        required:
        - spec
        title: MachineConfig
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: configpolicies.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: ConfigPolicy
    plural: configpolicies
    shortNames:
    - cpol
    singular: configpolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.compliantCount
      name: Compliant
      type: integer
    - jsonPath: .status.nonCompliantCount
      name: NonCompliant
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Enforced")].status
      name: Enforced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ConfigPolicySpec via `CustomResource`
        properties:
          spec:
            properties:
              debugModules:
                default: []
                description: Modules staged as debug-only (CSI volume without volumeMount on declared containers).
                items:
                  description: Reference to a module that should be installed on the machine.
                  properties:
                    name:
                      description: Name of the cluster-scoped `Module` resource to install.
                      type: string
                    required:
                      default: false
                      description: |-
                        Whether a failure to resolve or install this module fails the whole
                        reconcile. Default: `false`, so a missing module is reported and the
                        rest of the machine still converges.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              packages:
                default: []
                description: |-
                  Packages every selected MachineConfig must declare, each optionally
                  version-pinned.
                items:
                  description: Reference to a package with optional version pin.
                  properties:
                    name:
                      description: Package name as the machine's own package manager knows it.
                      type: string
                    version:
                      description: |-
                        Exact version to pin to. Omitted, whatever the manager currently offers
                        is installed and left alone once present.
                      nullable: true
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              requiredModules:
                default: []
                description: |-
                  Modules every selected MachineConfig must carry. A machine missing one
                  is counted non-compliant.
                items:
                  description: Reference to a module that should be installed on the machine.
                  properties:
                    name:
                      description: Name of the cluster-scoped `Module` resource to install.
                      type: string
                    required:
                      default: false
                      description: |-
                        Whether a failure to resolve or install this module fails the whole
                        reconcile. Default: `false`, so a missing module is reported and the
                        rest of the machine still converges.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              settings:
                additionalProperties: true
                default: {}
                description: |-
                  System settings every selected MachineConfig must declare, keyed the
                  same way as `MachineConfig.spec.systemSettings`.
                type: object
              targetSelector:
                default:
                  matchExpressions: []
                  matchLabels: {}
                description: |-
                  Which MachineConfigs in this namespace the policy applies to. Empty,
                  it applies to all of them.
                properties:
                  matchExpressions:
                    default: []
                    description: |-
                      Set-based requirements a resource must satisfy, evaluated alongside
                      `matchLabels`. Every requirement must hold.
                    items:
                      description: A single requirement for label selector expressions.
                      properties:
                        key:
                          description: Label key this requirement tests.
                          type: string
                        operator:
                          description: |-
                            How `values` is compared against the key: `In`, `NotIn`, `Exists`, or
                            `DoesNotExist`.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          default: []
                          description: |-
                            Values the key is tested against. Required for `In` and `NotIn`, and
                            must be empty for `Exists` and `DoesNotExist`.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - key
                    x-kubernetes-list-type: map
                  matchLabels:
                    additionalProperties:
                      type: string
                    default: {}
                    description: |-
                      Labels a resource must carry verbatim to match. Every entry must match;
                      an empty map matches everything.
                    type: object
                type: object
            type: object
          status:
            nullable: true
            properties:
              compliantCount:
                format: uint32
                minimum: 0.0
                type: integer
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nonCompliantCount:
                format: uint32
                minimum: 0.0
                type: integer
              nonCompliantMachines:
                default: []
                description: |-
                  `namespace/name` of the MachineConfigs currently violating this policy,
                  sorted and capped at [`MAX_NON_COMPLIANT_MACHINES`]. Persisted so a
                  `PolicyViolation` event fires on the transition into violation rather
                  than once per observation — an in-process memory would re-announce every
                  machine after an operator restart. `nonCompliantCount` is the exact
                  total and is never capped.
                items:
                  type: string
                maxItems: 500
                type: array
            required:
            - compliantCount
            - nonCompliantCount
            type: object
        required:
        - spec
        title: ConfigPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: driftalerts.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: DriftAlert
    plural: driftalerts
    shortNames:
    - da
    singular: driftalert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.deviceId
      name: Device
      type: string
    - jsonPath: .spec.severity
      name: Severity
      type: string
    - jsonPath: .status.conditions[?(@.type=="Resolved")].status
      name: Resolved
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for DriftAlertSpec via `CustomResource`
        properties:
          spec:
            description: |-
              Drifted system settings a device reported at check-in.

              A device's report covers its system settings alone — the answers of the
              system configurators its profile declares (`sysctl`, `kernelModules`,
              `macosDefaults`, `windowsRegistry`, ...). Packages, managed files, env vars
              and aliases are checked on the device by `cfgd diff` and reach the fleet only
              as the aggregate counts of a compliance summary, never as findings — so a
              device with no DriftAlert is a device whose system settings matched, not a
              device proven in sync.
            properties:
              deviceId:
                description: Identifier of the device that reported the drift, as it enrolled.
                type: string
              driftDetails:
                default: []
                description: |-
                  One entry per drifted system setting, each carrying what was declared and
                  what the device found.
                items:
                  description: One system setting whose live value diverged from its declared value.
                  properties:
                    actual:
                      description: The value the device actually reported.
                      type: string
                    expected:
                      description: The value the MachineConfig declares.
                      type: string
                    field:
                      description: |-
                        The drifted setting, named `<configurator>.<key>` as the device reported
                        it (e.g. `sysctl.net.ipv4.ip_forward`).

                        The configurator qualifies the key because a key names a setting only
                        within its own configurator: two of them may declare the same one, and
                        this list merges by this field.
                      type: string
                  required:
                  - actual
                  - expected
                  - field
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - field
                x-kubernetes-list-type: map
              machineConfigRef:
                description: The MachineConfig whose desired state the device diverged from.
                properties:
                  name:
                    description: Name of the referenced MachineConfig.
                    type: string
                  namespace:
                    description: |-
                      Namespace the MachineConfig lives in. Omitted, the referring
                      resource's own namespace is used.
                    nullable: true
                    type: string
                required:
                - name
                type: object
              severity:
                description: |-
                  How serious the divergence is: `Low`, `Medium`, `High`, or `Critical`.
                  Drives alert routing rather than any automatic remediation.
                enum:
                - Low
                - Medium
                - High
                - Critical
                type: string
            required:
            - deviceId
            - machineConfigRef
            - severity
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              detectedAt:
                nullable: true
                type: string
              resolvedAt:
                nullable: true
                type: string
            type: object
        required:
        - spec
        title: DriftAlert
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: clusterconfigpolicies.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: ClusterConfigPolicy
    plural: clusterconfigpolicies
    shortNames:
    - ccpol
    singular: clusterconfigpolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.compliantCount
      name: Compliant
      type: integer
    - jsonPath: .status.nonCompliantCount
      name: NonCompliant
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Enforced")].status
      name: Enforced
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ClusterConfigPolicySpec via `CustomResource`
        properties:
          spec:
            properties:
              debugModules:
                default: []
                description: Modules staged as debug-only across matching namespaces.
                items:
                  description: Reference to a module that should be installed on the machine.
                  properties:
                    name:
                      description: Name of the cluster-scoped `Module` resource to install.
                      type: string
                    required:
                      default: false
                      description: |-
                        Whether a failure to resolve or install this module fails the whole
                        reconcile. Default: `false`, so a missing module is reported and the
                        rest of the machine still converges.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              namespaceSelector:
                default:
                  matchExpressions: []
                  matchLabels: {}
                description: |-
                  Select which namespaces this cluster policy applies to. Empty, it
                  applies to every namespace in the cluster.
                properties:
                  matchExpressions:
                    default: []
                    description: |-
                      Set-based requirements a resource must satisfy, evaluated alongside
                      `matchLabels`. Every requirement must hold.
                    items:
                      description: A single requirement for label selector expressions.
                      properties:
                        key:
                          description: Label key this requirement tests.
                          type: string
                        operator:
                          description: |-
                            How `values` is compared against the key: `In`, `NotIn`, `Exists`, or
                            `DoesNotExist`.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          default: []
                          description: |-
                            Values the key is tested against. Required for `In` and `NotIn`, and
                            must be empty for `Exists` and `DoesNotExist`.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-map-keys:
                    - key
                    x-kubernetes-list-type: map
                  matchLabels:
                    additionalProperties:
                      type: string
                    default: {}
                    description: |-
                      Labels a resource must carry verbatim to match. Every entry must match;
                      an empty map matches everything.
                    type: object
                type: object
              packages:
                default: []
                description: Packages every MachineConfig in a selected namespace must declare.
                items:
                  description: Reference to a package with optional version pin.
                  properties:
                    name:
                      description: Package name as the machine's own package manager knows it.
                      type: string
                    version:
                      description: |-
                        Exact version to pin to. Omitted, whatever the manager currently offers
                        is installed and left alone once present.
                      nullable: true
                      type: string
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              requiredModules:
                default: []
                description: Modules every MachineConfig in a selected namespace must carry.
                items:
                  description: Reference to a module that should be installed on the machine.
                  properties:
                    name:
                      description: Name of the cluster-scoped `Module` resource to install.
                      type: string
                    required:
                      default: false
                      description: |-
                        Whether a failure to resolve or install this module fails the whole
                        reconcile. Default: `false`, so a missing module is reported and the
                        rest of the machine still converges.
                      type: boolean
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              security:
                default:
                  allowUnsigned: false
                  trustedRegistries: []
                description: Cluster-wide rules governing which modules may be admitted at all.
                properties:
                  allowUnsigned:
                    default: false
                    description: |-
                      Whether a module carrying an unsigned OCI artifact may be admitted.
                      Default: `false` — creating any ClusterConfigPolicy at all starts
                      rejecting unsigned modules.
                    type: boolean
                  trustedRegistries:
                    default: []
                    description: |-
                      Registry prefixes a module artifact may be pulled from; a trailing `*`
                      or `/` widens the match. Empty, any registry is accepted.
                    items:
                      type: string
                    type: array
                    x-kubernetes-list-type: set
                type: object
              settings:
                additionalProperties: true
                default: {}
                description: |-
                  System settings every MachineConfig in a selected namespace must
                  declare, keyed the same way as `MachineConfig.spec.systemSettings`.
                type: object
            type: object
          status:
            nullable: true
            properties:
              compliantCount:
                format: uint32
                minimum: 0.0
                type: integer
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              nonCompliantCount:
                format: uint32
                minimum: 0.0
                type: integer
              nonCompliantMachines:
                default: []
                description: |-
                  `namespace/name` of the MachineConfigs currently violating this policy,
                  sorted and capped at [`MAX_NON_COMPLIANT_MACHINES`]. Persisted so a
                  `PolicyViolation` event fires on the transition into violation rather
                  than once per observation — an in-process memory would re-announce every
                  machine after an operator restart. `nonCompliantCount` is the exact
                  total and is never capped.
                items:
                  type: string
                maxItems: 500
                type: array
            required:
            - compliantCount
            - nonCompliantCount
            type: object
        required:
        - spec
        title: ClusterConfigPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: modules.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: Module
    plural: modules
    shortNames:
    - mod
    singular: module
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.ociArtifact
      name: Artifact
      type: string
    - jsonPath: .status.signature
      name: Signature
      type: string
    - jsonPath: .status.platformsSummary
      name: Platforms
      type: string
    - jsonPath: .status.conditions[?(@.type=="Available")].status
      name: Available
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for ModuleSpec via `CustomResource`
        properties:
          spec:
            properties:
              aliases:
                description: Shell aliases this module contributes to the machines that apply it.
                items:
                  description: A shell alias a Module contributes.
                  properties:
                    command:
                      description: |-
                        Command the alias expands to, written in the syntax of the shell it is
                        generated for. cfgd quotes the whole value per dialect when it writes
                        the alias definition, so the text reaches the shell exactly as declared.
                      type: string
                    name:
                      description: Alias name, as typed at the shell prompt.
                      type: string
                    platforms:
                      description: |-
                        Platform tags gating this entry alone. Empty means every platform the
                        declaring module is not already gated off of. Tags are matched against
                        the machine's OS, distro, and arch; use `macos` for macOS.
                      items:
                        type: string
                      type: array
                  required:
                  - command
                  - name
                  type: object
                type: array
              depends:
                default: []
                description: Names of other `Module` resources that must be applied first.
                items:
                  type: string
                type: array
                x-kubernetes-list-type: set
              env:
                default: []
                description: |-
                  Environment variables this module contributes to containers that mount
                  it.
                items:
                  description: An environment variable set by a Module.
                  properties:
                    append:
                      default: false
                      description: |-
                        Append to the variable's existing value with the platform's path
                        separator instead of replacing it. Default: `false`.
                      type: boolean
                    name:
                      description: Variable name, as exported into the container or shell environment.
                      type: string
                    platforms:
                      description: |-
                        Platform tags gating this entry alone. Empty means every platform the
                        declaring module is not already gated off of. Tags are matched against
                        the machine's OS, distro, and arch; use `macos` for macOS. A pod is a
                        Linux container, so the pod-mutating webhook injects an entry only when
                        this is empty or names `linux` — nothing about a pod can answer a
                        distro or arch tag.
                      items:
                        type: string
                      type: array
                    value:
                      description: Value assigned to the variable.
                      type: string
                  required:
                  - name
                  - value
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              files:
                default: []
                description: Files this module deploys out of its artifact.
                items:
                  description: A file managed by a Module.
                  properties:
                    encryption:
                      description: |-
                        Encryption the source file must satisfy. Rejected on a `Patch` entry,
                        which has no source to encrypt.
                      nullable: true
                      properties:
                        backend:
                          description: The encryption backend to use (e.g. "sops", "age").
                          type: string
                        mode:
                          default: InRepo
                          description: When encryption must be enforced. Defaults to `InRepo`.
                          enum:
                          - InRepo
                          - Always
                          type: string
                      required:
                      - backend
                      type: object
                    patch:
                      description: |-
                        Structured merge or rewriting script for `strategy: Patch`. Required
                        when `strategy` is `Patch`, rejected otherwise.
                      nullable: true
                      properties:
                        ensure:
                          description: |-
                            Keys/values to deep-merge into the target, leaving unmentioned keys
                            untouched. Values are literal (no template rendering). Mutually
                            exclusive with `script`.
                          x-kubernetes-preserve-unknown-fields: true
                        format:
                          description: File format used to interpret and re-serialize a `Patch`-strategy target.
                          enum:
                          - Ini
                          - Json
                          - Yaml
                          - Toml
                          nullable: true
                          type: string
                        script:
                          description: |-
                            A script path or an inline command that receives the target's current
                            content on stdin and writes the new content to stdout. A relative path
                            resolves against the module directory for a module file
                            (`spec.files[]`) and against the config directory for a profile file
                            (`spec.files.managed[]`); a value that resolves to no file is run as an
                            inline command. Mutually exclusive with `ensure`.
                          nullable: true
                          type: string
                      type: object
                    permissions:
                      description: Unix permission bits (e.g. "600", "644") applied after deployment.
                      nullable: true
                      type: string
                    private:
                      description: |-
                        The source file is local-only: auto-added to .gitignore, and silently
                        skipped on a machine where it does not exist. Default: `false`.
                      type: boolean
                    source:
                      default: ''
                      description: |-
                        Path to the file inside the module artifact. Empty only when
                        `strategy` is `Patch`, which rewrites the target's own content and
                        reads no source.
                      type: string
                    strategy:
                      description: File deployment strategy.
                      enum:
                      - Symlink
                      - Copy
                      - Template
                      - Hardlink
                      - Patch
                      nullable: true
                      type: string
                    target:
                      description: |-
                        Destination path the file is deployed to inside the pod or on the
                        machine.
                      type: string
                  required:
                  - target
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - target
                x-kubernetes-list-type: map
              hooks:
                description: |-
                  Lifecycle hooks the cfgd agent runs around this module's deployment,
                  each a list of inline command bodies with their own guards and
                  timeouts. Distinct from `scripts.postApply`, which is a script PATH
                  inside the artifact that the pod-mutating webhook runs in an init
                  container: these bodies are for the agent applying the module to a
                  machine, and nothing in a pod runs them.
                nullable: true
                properties:
                  onChange:
                    default: []
                    description: Run when a watched file changes on disk (requires `daemon.reconcile.onChange`).
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  onDrift:
                    default: []
                    description: Run when the daemon detects drift, before any auto-apply decision.
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  postApply:
                    default: []
                    description: Run once after every action in an apply completes.
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  postReconcile:
                    default: []
                    description: Run once after a daemon reconcile tick completes.
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  preApply:
                    default: []
                    description: Run once before any action in an apply.
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                  preReconcile:
                    default: []
                    description: Run once before a daemon reconcile tick begins.
                    items:
                      anyOf:
                      - {}
                      - required:
                        - run
                      description: |-
                        A lifecycle script entry: either a bare command string, or a mapping for
                        one that needs a timeout, shell, or guard condition.

                        ```yaml
                        preApply: "echo starting"
                        # or
                        postApply:
                          run: brew update
                          timeout: 2m
                          onlyIf: command -v brew
                        ```
                      properties:
                        continueOnError:
                          description: |-
                            Treat a non-zero exit as success and continue reconciliation instead
                            of failing the run. Default: `false`.
                          nullable: true
                          type: boolean
                        creates:
                          description: |-
                            Skip the script if this path already exists. A leading `~` expands to
                            the home directory; a relative path resolves against the script's
                            working directory. Existence follows symlinks.
                          nullable: true
                          type: string
                        idleTimeout:
                          description: |-
                            Kill the script if it produces no stdout/stderr output for this duration.
                            Prevents scripts from silently hanging on unresponsive resources.
                            Format: "30s", "2m", etc. If unset, no idle timeout is enforced.
                          nullable: true
                          type: string
                        interactive:
                          description: |-
                            Run the script attached to the terminal (inherited stdin/stdout/stderr,
                            no spinner, no output capture, no idle timeout) so it can prompt the
                            user — e.g. `echo "press Enter when done"; read`. Requires a TTY: when
                            stdin is not a terminal (CI, piped input, or any daemon-run phase) the
                            script is skipped with a warning rather than hanging on instant EOF.
                          type: boolean
                        onlyIf:
                          description: |-
                            Run the script only if this command exits zero. A non-zero exit skips
                            the script (the condition for running was not met). Evaluated with the
                            same shell, working directory, and environment as the body.
                          nullable: true
                          type: string
                        run:
                          description: The command or script body to run.
                          type: string
                        shell:
                          description: Interpreter to use for inline commands. Ignored (and rejected) on file scripts.
                          enum:
                          - sh
                          - bash
                          - zsh
                          - pwsh
                          - cmd
                          - auto
                          type: string
                        timeout:
                          description: |-
                            Kill the script if it runs longer than this duration (`"30s"`, `"2m"`).
                            Unset means no timeout.
                          nullable: true
                          type: string
                        unless:
                          description: |-
                            Run the script only if this command exits NON-zero. A zero exit
                            (success) skips the script (the guarded state already holds).
                            Evaluated with the same shell, working directory, and environment as
                            the body.
                          nullable: true
                          type: string
                        workdir:
                          description: |-
                            Working directory for the script. By default every lifecycle script
                            runs in the user's home directory — never the config source tree — so
                            a relative write can't pollute the user's GitOps repo. Set `workdir`
                            to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand
                            against the script environment (which always carries `$CFGD_MODULE_DIR`
                            and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,
                            `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.
                          nullable: true
                          type: string
                      x-kubernetes-preserve-unknown-fields: true
                    type: array
                type: object
              mountPolicy:
                default: Always
                description: |-
                  Controls how the module is mounted in pods.
                  `Always` (default): CSI volume + volumeMount on all declared containers + env vars.
                  `Debug`: CSI volume only — no volumeMount or env on declared containers.
                    Only accessible via ephemeral debug containers (`kubectl cfgd debug`).
                enum:
                - Always
                - Debug
                type: string
              ociArtifact:
                description: |-
                  OCI reference the module's content is pulled from
                  (`ghcr.io/acme/nvim:1.2.0`). Omitted, the module carries its content
                  inline and nothing is fetched.
                nullable: true
                type: string
              packages:
                default: []
                description: |-
                  Packages this module installs. Each entry names the package and the
                  hints that decide which manager installs it: per-manager name
                  overrides, a version floor, a manager preference order, a manager
                  denylist, and the platform tags gating the entry.
                items:
                  description: |-
                    An entry in a Module's package list: the package to install plus the hints
                    that decide which manager on a machine installs it.
                  properties:
                    aliases:
                      additionalProperties:
                        type: string
                      description: |-
                        Per-manager package name overrides (e.g. {"brew": "gnu-sed", "apt": "sed"})
                        for a package spelled differently by each manager.
                      type: object
                    deny:
                      description: |-
                        Package managers never used for this package, even where one is
                        available and the machine's own priority would pick it.
                      items:
                        type: string
                      type: array
                    minVersion:
                      description: |-
                        Minimum acceptable installed version, loosely parsed (`"1.2"`, `"1"`).
                        An installed copy below this is treated as not satisfying the module.
                      nullable: true
                      type: string
                    name:
                      description: |-
                        Default package name, used on every manager `aliases` names no
                        override for.
                      type: string
                    platforms:
                      description: |-
                        Platform tags gating this package alone. Empty means every platform the
                        declaring module is not already gated off of. Tags are matched against
                        the machine's OS, distro, and arch; use `macos` for macOS.
                      items:
                        type: string
                      type: array
                    prefer:
                      description: |-
                        Manager preference order for this package, overriding the machine's
                        default manager priority (e.g. `[brew, apt]`).
                      items:
                        type: string
                      type: array
                  required:
                  - name
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              platforms:
                description: |-
                  Platform tags gating the whole module on a machine reconciling it.
                  When non-empty and the machine matches none of them, the module is
                  skipped entirely (it appears as a skipped action rather than
                  vanishing). Tags are matched against the machine's OS, distro, and
                  arch; use `macos` for macOS.
                items:
                  type: string
                type: array
              scripts:
                default: {}
                description: Lifecycle commands run around the module's deployment.
                properties:
                  postApply:
                    description: |-
                      Command run once after the module's files and packages are in place.
                      Omitted, nothing runs.
                    nullable: true
                    type: string
                type: object
              signature:
                description: |-
                  How the artifact's provenance is verified before it is admitted.
                  Omitted, verification is governed by the cluster policy alone.
                nullable: true
                properties:
                  cosign:
                    description: |-
                      How the artifact's cosign signature is verified. Omitted, no signature
                      is required of this module.
                    nullable: true
                    properties:
                      certificateIdentity:
                        description: Certificate identity pattern for keyless verification (regex).
                        nullable: true
                        type: string
                      certificateOidcIssuer:
                        description: Certificate OIDC issuer pattern for keyless verification (regex).
                        nullable: true
                        type: string
                      keyless:
                        default: false
                        description: Enable keyless verification via Fulcio/Rekor (OIDC identity-based).
                        type: boolean
                      publicKey:
                        description: PEM-encoded public key for static key verification.
                        nullable: true
                        type: string
                    type: object
                type: object
              system:
                additionalProperties: true
                description: |-
                  System configurator settings this module contributes, keyed by
                  configurator name (`shell`, `sysctl`, `macosDefaults`, …). Deep-merged
                  into the profile's system map, module values winning at leaf level.
                type: object
            type: object
          status:
            nullable: true
            properties:
              attestations:
                description: Attestation types found on the artifact (e.g. "slsaprovenance1").
                items:
                  type: string
                type: array
              availablePlatforms:
                default: []
                items:
                  type: string
                type: array
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  The `metadata.generation` every other field here was computed from. A
                  status whose `observedGeneration` is behind `metadata.generation`
                  describes the PREVIOUS spec, which is what lets a caller wait for a
                  verdict about the spec it just applied instead of reading the one it
                  replaced.
                format: int64
                nullable: true
                type: integer
              platformsSummary:
                description: |-
                  `availablePlatforms` rendered as one comma-joined string for the
                  `Platforms` printer column, and the only field that column may be bound
                  to: a column resolving to an array prints the Go rendering of the
                  slice, so an empty one reads as the literal `[]` where an absent value
                  leaves the cell empty. Absent when no platform is known, so the
                  JSONPath resolves to nothing and the cell stays blank.
                nullable: true
                type: string
              resolvedArtifact:
                nullable: true
                type: string
              signature:
                description: |-
                  The signature verdict as ONE word (`verified` / `unverified` /
                  `unsigned` / `unknown`), and the only field the `Signature` printer
                  column may be bound to. `verified` is the same verdict as a raw bool,
                  which reads as
                  `true` in a column beside a `kubectl cfgd status` row saying
                  `(verified)` about the same module — one fact, two vocabularies. Absent
                  when no reconcile has written it, so the JSONPath resolves to nothing
                  and the cell stays blank.
                nullable: true
                type: string
              signatureDigest:
                description: Digest of the cosign signature (if verified).
                nullable: true
                type: string
              verified:
                default: false
                type: boolean
            type: object
        required:
        - spec
        title: Module
        type: object
    served: true
    storage: true
    subresources:
      status: {}
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: backuppolicies.cfgd.io
spec:
  group: cfgd.io
  names:
    categories:
    - cfgd
    kind: BackupPolicy
    plural: backuppolicies
    shortNames:
    - bpol
    singular: backuppolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.unitsSummary
      name: Units
      type: string
    - jsonPath: .status.machinesMatched
      name: Machines
      type: integer
    - jsonPath: .status.conditions[?(@.type=="Applied")].status
      name: Applied
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Auto-generated derived type for BackupPolicySpec via `CustomResource`
        properties:
          spec:
            description: |-
              A fleet-wide backup schedule, applied to the machines its selector matches.

              A policy overrides the cadence of a backup unit the machine's own profile
              already defines; it never defines the unit. `source` and `destination` are
              machine-local paths a cluster object cannot know, so a unit this policy
              names on a machine whose profile does not define it is reported and
              otherwise left alone.
            properties:
              selector:
                default:
                  matchExpressions: []
                  matchLabels: {}
                description: |-
                  Which MachineConfigs in this namespace the policy schedules backups
                  for. Empty, it applies to all of them.
                properties:
                  matchExpressions:
                    default: []
                    description: |-
                      Set-based requirements a resource must satisfy, evaluated alongside
                      `matchLabels`. Every requirement must hold.
                    items:
                      description: A single requirement for label selector expressions.
                      properties:
                        key:
                          description: Label key this requirement tests.
                          type: string
                        operator:
                          description: |-
                            How `values` is compared against the key: `In`, `NotIn`, `Exists`, or
                            `DoesNotExist`.
                          enum:
                          - In
                          - NotIn
                          - Exists
                          - DoesNotExist
                          type: string
                        values:
                          default: []
                          description: |-
                            Values the key is tested against. Required for `In` and `NotIn`, and
                            must be empty for `Exists` and `DoesNotExist`.
                          items:
                            type: string
                          type: array
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                  matchLabels:
                    additionalProperties:
                      type: string
                    default: {}
                    description: |-
                      Labels a resource must carry verbatim to match. Every entry must match;
                      an empty map matches everything.
                    type: object
                type: object
              units:
                default: []
                description: |-
                  Schedule overrides, each naming a backup unit the matched machine's own
                  profile defines.
                items:
                  description: One backup unit's cluster-side schedule.
                  properties:
                    name:
                      description: |-
                        Name of the unit in the machine's own `spec.backups[]` this entry
                        schedules. Unique within the list.
                      type: string
                    retention:
                      description: |-
                        How many snapshots the unit keeps. Omitted, the machine's own profile
                        decides.
                      format: uint32
                      minimum: 0.0
                      nullable: true
                      type: integer
                    schedule:
                      description: |-
                        Cron expression (`0 3 * * *`) or interval (`6h`) the unit runs on,
                        evaluated against the machine's local clock — a nightly window means
                        3am where each machine sits, not 3am in the cluster's timezone.
                      type: string
                  required:
                  - name
                  - schedule
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            type: object
          status:
            nullable: true
            properties:
              conditions:
                default: []
                items:
                  properties:
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    observedGeneration:
                      format: int64
                      nullable: true
                      type: integer
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              machinesMatched:
                description: How many machines the selector matched, exact and never capped.
                format: uint32
                minimum: 0.0
                type: integer
              observedGeneration:
                description: |-
                  The `metadata.generation` every other field here was computed from. A
                  status whose `observedGeneration` is behind `metadata.generation`
                  describes the PREVIOUS spec.
                format: int64
                nullable: true
                type: integer
              units:
                default: []
                description: |-
                  Sorted by (hostname, name) and capped at MAX_NON_COMPLIANT_MACHINES — the shared
                  etcd enumeration ceiling; `machinesMatched` stays exact.
                items:
                  description: One machine's copy of one backup unit, as the policy last observed it.
                  properties:
                    hostname:
                      description: Hostname of the machine this row reports on.
                      type: string
                    lastRun:
                      description: When the unit last ran, as an RFC 3339 timestamp.
                      nullable: true
                      type: string
                    message:
                      description: |-
                        Why this row is not what the policy asked for: the unit is unknown to
                        the machine's profile, or its schedule is pinned local.
                      nullable: true
                      type: string
                    name:
                      description: Name of the backup unit.
                      type: string
                    nextRun:
                      description: When the unit is next due, as an RFC 3339 timestamp.
                      nullable: true
                      type: string
                    owner:
                      description: |-
                        Which layer owns this unit's schedule on that machine: `cluster` when
                        this policy's schedule projects onto it, `local` when the machine's own
                        profile pinned the unit with `scheduleOwner: Local` and the policy
                        reports it without scheduling it.
                      type: string
                    retention:
                      description: The retention in force on the machine.
                      format: uint32
                      minimum: 0.0
                      nullable: true
                      type: integer
                    schedule:
                      description: |-
                        The schedule in force on the machine. Absent while the machine has not
                        reported the unit yet.
                      nullable: true
                      type: string
                  required:
                  - hostname
                  - name
                  - owner
                  type: object
                maxItems: 500
                type: array
                x-kubernetes-list-map-keys:
                - hostname
                - name
                x-kubernetes-list-type: map
              unitsSummary:
                description: |-
                  The unit names in `units`, deduplicated and comma-joined, and the only
                  field the `Units` printer column may be bound to: a column resolving to
                  an array prints the Go rendering of the slice, so an empty one reads as
                  the literal `[]` where an absent value leaves the cell empty. Absent
                  when no unit has been reported.
                nullable: true
                type: string
            required:
            - machinesMatched
            type: object
        required:
        - spec
        title: BackupPolicy
        type: object
    served: true
    storage: true
    subresources:
      status: {}
