apiVersion: cfgd.io/v1alpha1
kind: Profile
metadata:
  name: base
spec:
  env:
    - name: EDITOR
      value: vim
    - name: SHELL
      value: /bin/bash
  files:
    managed:
      - source: files/gitconfig
        target: TARGET_DIR/.gitconfig
  packages:
    brew:
      formulae: []
