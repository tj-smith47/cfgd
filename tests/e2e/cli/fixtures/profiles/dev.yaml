apiVersion: cfgd.io/v1alpha1
kind: Profile
metadata:
  name: dev
spec:
  inherits:
    - base
  variables:
    EDITOR: nvim
  files:
    managed:
      - source: files/zshrc
        target: TARGET_DIR/.zshrc
