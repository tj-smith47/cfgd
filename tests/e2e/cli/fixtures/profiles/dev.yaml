apiVersion: cfgd.io/v1alpha1
kind: Profile
metadata:
  name: dev
spec:
  inherits:
    - base
  env:
    - name: EDITOR
      value: nvim
  files:
    managed:
      - source: files/zshrc
        target: TARGET_DIR/.zshrc
