{{- if and .Values.webhook.enabled .Values.webhook.certManager.enabled }}
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ include "cfgd-operator.fullname" . }}-selfsigned-issuer
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cfgd-operator.labels" . | nindent 4 }}
spec:
  selfSigned: {}
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: {{ include "cfgd-operator.webhookCertSecret" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cfgd-operator.labels" . | nindent 4 }}
spec:
  secretName: {{ include "cfgd-operator.webhookCertSecret" . }}
  issuerRef:
    name: {{ include "cfgd-operator.fullname" . }}-selfsigned-issuer
    kind: Issuer
  dnsNames:
    - {{ include "cfgd-operator.fullname" . }}-webhook
    - {{ include "cfgd-operator.fullname" . }}-webhook.{{ .Release.Namespace }}
    - {{ include "cfgd-operator.fullname" . }}-webhook.{{ .Release.Namespace }}.svc
    - {{ include "cfgd-operator.fullname" . }}-webhook.{{ .Release.Namespace }}.svc.cluster.local
  duration: 8760h  # 1 year
  renewBefore: 720h  # 30 days
{{- end }}
