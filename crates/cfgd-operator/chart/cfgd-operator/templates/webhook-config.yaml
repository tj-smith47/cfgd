{{- if .Values.webhook.enabled }}
apiVersion: admissionregistration.k8s.io/v1
kind: ValidatingWebhookConfiguration
metadata:
  name: {{ include "cfgd-operator.fullname" . }}
  labels:
    {{- include "cfgd-operator.labels" . | nindent 4 }}
  {{- if .Values.webhook.certManager.enabled }}
  annotations:
    cert-manager.io/inject-ca-from: {{ .Release.Namespace }}/{{ include "cfgd-operator.webhookCertSecret" . }}
  {{- end }}
webhooks:
  - name: validate-machineconfig.cfgd.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    timeoutSeconds: {{ .Values.webhook.timeoutSeconds }}
    clientConfig:
      service:
        name: {{ include "cfgd-operator.fullname" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: /validate-machineconfig
    rules:
      - apiGroups: ["cfgd.io"]
        apiVersions: ["v1alpha1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["machineconfigs"]
  - name: validate-configpolicy.cfgd.io
    admissionReviewVersions: ["v1"]
    sideEffects: None
    failurePolicy: {{ .Values.webhook.failurePolicy }}
    timeoutSeconds: {{ .Values.webhook.timeoutSeconds }}
    clientConfig:
      service:
        name: {{ include "cfgd-operator.fullname" . }}-webhook
        namespace: {{ .Release.Namespace }}
        path: /validate-configpolicy
    rules:
      - apiGroups: ["cfgd.io"]
        apiVersions: ["v1alpha1"]
        operations: ["CREATE", "UPDATE"]
        resources: ["configpolicies"]
{{- end }}
