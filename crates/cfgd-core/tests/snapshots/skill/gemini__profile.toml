cfgd-min-version = "<CFGD_MIN_VERSION>"
cfgd-version = "<CFGD_VERSION>"
description = "Investigate thoroughly and author a complete, validated cfgd Profile resource."
prompt = '''
<!-- cfgd-version: <CFGD_VERSION> · cfgd-min-version: <CFGD_MIN_VERSION> -->

# Author a high-quality cfgd Profile

Follow this protocol on every invocation. The quality bar is NOT "valid YAML". It is exhaustive field evaluation, external research, and a documented rationale for every choice. A box-checking resource (every field technically present, no investigation behind it) fails this bar. Evaluate EVERY field the kind exposes; for each, either populate it with a justified value or omit it only after investigating enough to conclude it does not apply. Ground every version, ordering, and strategy choice in evidence, never a guess.

## Protocol

0. **Precondition — confirm the toolchain is usable.** Run `command -v cfgd`; if it is absent, STOP and tell the user to install cfgd >= <CFGD_MIN_VERSION>. Run `cfgd --version`; if it is older than <CFGD_MIN_VERSION>, warn and prefer the embedded fallback schema below.
1. **Enumerate every field for this kind (live-first, snapshot-fallback).** Run `cfgd explain profile -o json` for the authoritative live schema, and `cfgd explain profile.<field> -o json` to drill into nested objects. If cfgd is absent or older than the stamp, use the embedded fallback schema below (stamped <CFGD_VERSION>).
2. **Research best practices externally for THIS subject.** For each field, consult external best practice before settling a value: the tool's own docs, the package managers that ship it, and community conventions. Record what you verified and your confidence level when a source was unavailable. Prefer live evidence over training-knowledge recall, and state explicitly when you could not confirm a claim.
3. **For EVERY field, decide include OR omit, and justify with a WHY comment.** Box-checking is a failure; meeting the rubric above is the target.
4. **Draft thoroughly:** transitive deps explicit, version constraints set, platforms scoped, multi-step scripts idempotent (timeout + continueOnError), comments-as-specification.
5. **Validate against the schema:** `cfgd profile validate <file>` — fix until clean (validate against the embedded snapshot if cfgd is unavailable).
6. **Self-critique against the rubric:** "Box-checking or thorough? Which field did I skip, and was that deliberate?" Iterate until the answer holds.

## Ground-truth examples

```yaml
apiVersion: cfgd.io/v1alpha1
kind: Profile
metadata:
  name: base
spec:
  env:
    - name: EDITOR
      value: vim
    - name: GIT_AUTHOR_NAME
      value: Your Name
    - name: GIT_AUTHOR_EMAIL
      value: you@example.com

  packages:
    cargo:
      - bat
      - eza
      - fd-find
      - ripgrep

  files:
    managed:
      - source: shell/.zshrc
        target: ~/.zshrc
      - source: git/.gitconfig.tera
        target: ~/.gitconfig
    permissions:
      "~/.ssh": "700"
      "~/.ssh/config": "600"

  system:
    shell: /bin/zsh
```

```yaml
apiVersion: cfgd.io/v1alpha1
kind: Profile
metadata:
  name: work
spec:
  inherits:
    - base

  env:
    - name: EDITOR
      value: "code --wait"
    - name: GIT_AUTHOR_EMAIL
      value: you@company.com

  packages:
    brew:
      taps:
        - homebrew/cask-fonts
      formulae:
        - git
        - jq
        - kubectl
        - helm
      casks:
        - visual-studio-code
        - wezterm
    npm:
      global:
        - typescript
        - prettier

  files:
    managed:
      - source: k8s/kubeconfig.tera
        target: ~/.kube/config

  secrets:
    - source: 1password://Work/GitHub/token
      target: ~/.config/gh/token

  scripts:
    postReconcile:
      - scripts/setup-work-env.sh
```

## Fallback schema (if cfgd is unavailable)

Generated against cfgd <CFGD_VERSION>. Live `cfgd explain profile` is authoritative when present.

```json
{"$schema":"https://json-schema.org/draft/2020-12/schema","title":"ProfileSpec","type":"object","properties":{"aliases":{"type":"array","default":[],"items":{"$ref":"#/$defs/ShellAlias"}},"env":{"type":"array","default":[],"items":{"$ref":"#/$defs/EnvVar"}},"envScope":{"description":"How far `spec.env` exports reach across the current user's environment.\nOmitted means \"inherit\" (a parent layer's value survives); the resolved\ndefault when no layer sets it is [`EnvScope::All`] — every standard user\nentry point cfgd can safely touch. Narrow it to `Login` or `Interactive`\nto opt out of the broader session surfaces.","anyOf":[{"$ref":"#/$defs/EnvScope"},{"type":"null"}]},"files":{"anyOf":[{"$ref":"#/$defs/FilesSpec"},{"type":"null"}],"default":null},"inherits":{"type":"array","default":[],"items":{"type":"string"}},"modules":{"type":"array","default":[],"items":{"type":"string"}},"packages":{"anyOf":[{"$ref":"#/$defs/PackagesSpec"},{"type":"null"}],"default":null},"scripts":{"anyOf":[{"$ref":"#/$defs/ScriptSpec"},{"type":"null"}],"default":null},"secrets":{"type":"array","default":[],"items":{"$ref":"#/$defs/SecretSpec"}},"system":{"type":"object","additionalProperties":true,"default":{}}},"additionalProperties":false,"$defs":{"AptSpec":{"type":"object","properties":{"file":{"type":["string","null"],"default":null},"packages":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false},"BrewSpec":{"type":"object","properties":{"casks":{"type":"array","default":[],"items":{"type":"string"}},"file":{"type":["string","null"],"default":null},"formulae":{"type":"array","default":[],"items":{"type":"string"}},"taps":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false},"CargoSpec":{"description":"Cargo package spec. Supports both list form (`cargo: [bat, ripgrep]`)\nand object form (`cargo: { file: Cargo.toml, packages: [...] }`) via the\nshared `list_or_struct` deserializer on the `PackagesSpec::cargo` field.","type":"object","properties":{"file":{"type":["string","null"]},"packages":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false},"CustomManagerSpec":{"type":"object","properties":{"check":{"type":"string"},"install":{"type":"string"},"listInstalled":{"type":"string"},"name":{"type":"string"},"packages":{"type":"array","default":[],"items":{"type":"string"}},"uninstall":{"type":"string"},"update":{"type":["string","null"],"default":null}},"additionalProperties":false,"required":["name","check","listInstalled","install","uninstall"]},"EncryptionMode":{"description":"Controls when encryption is required for a managed file.","oneOf":[{"description":"File must be encrypted when stored in the repository.","type":"string","const":"InRepo"},{"description":"File must always be encrypted, including at rest on disk.","type":"string","const":"Always"}]},"EncryptionSpec":{"description":"Encryption settings for a managed file.","type":"object","properties":{"backend":{"description":"The encryption backend to use (e.g. \"sops\", \"age\").","type":"string"},"mode":{"description":"When encryption must be enforced. Defaults to `InRepo`.","$ref":"#/$defs/EncryptionMode","default":"InRepo"}},"additionalProperties":false,"required":["backend"]},"EnvScope":{"description":"How far `spec.env` exports reach across the current user's environment.\n\nThe two env fields differ by *scope of affected users*: `spec.env` targets\nthe current user, `spec.system.environment` targets all users (privileged).\nThis knob narrows the *current-user* reach; it never widens beyond the user.","oneOf":[{"description":"Every standard user entry point cfgd can safely touch: interactive +\nlogin shells, `systemd --user` / Wayland GUI sessions, macOS GUI apps,\nand an immediate live-session refresh. The default — no gotchas.","type":"string","const":"All"},{"description":"Interactive shells plus login shells (`~/.zshenv`, `~/.profile`, and an\nexisting `~/.bash_profile`). Excludes the GUI / `systemd --user` session\nsurfaces and the live-session refresh.","type":"string","const":"Login"},{"description":"Interactive shells only (`~/.bashrc` / `~/.zshrc`, fish conf.d) — the\nhistorical behavior before full reach.","type":"string","const":"Interactive"}]},"EnvVar":{"type":"object","properties":{"name":{"type":"string"},"value":{"type":"string"}},"required":["name","value"]},"FileStrategy":{"description":"File deployment strategy.","oneOf":[{"description":"Create a symbolic link from target to source (default).","type":"string","const":"Symlink"},{"description":"Copy source content to target.","type":"string","const":"Copy"},{"description":"Render a Tera template and write the output (auto-selected for .tera files).","type":"string","const":"Template"},{"description":"Create a hard link from target to source.","type":"string","const":"Hardlink"}]},"FilesSpec":{"type":"object","properties":{"managed":{"type":"array","default":[],"items":{"$ref":"#/$defs/ManagedFileSpec"}},"permissions":{"type":"object","additionalProperties":{"type":"string"},"default":{}}},"additionalProperties":false},"FlatpakSpec":{"type":"object","properties":{"packages":{"type":"array","default":[],"items":{"type":"string"}},"remote":{"type":["string","null"],"default":null}},"additionalProperties":false},"ManagedFileSpec":{"type":"object","properties":{"encryption":{"description":"Encryption settings for this file.","anyOf":[{"$ref":"#/$defs/EncryptionSpec"},{"type":"null"}]},"permissions":{"description":"Unix permission bits (e.g. \"600\", \"644\") to apply after deployment.","type":["string","null"]},"private":{"description":"When true, the source file is local-only: auto-added to .gitignore,\nsilently skipped on machines where it doesn't exist.","type":"boolean"},"source":{"type":"string"},"strategy":{"description":"Per-file deployment strategy override. If None, uses the global default.","anyOf":[{"$ref":"#/$defs/FileStrategy"},{"type":"null"}]},"target":{"type":"string"}},"additionalProperties":false,"required":["source","target"]},"NpmSpec":{"type":"object","properties":{"file":{"type":["string","null"],"default":null},"global":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false},"PackagesSpec":{"type":"object","properties":{"apk":{"type":"array","default":[],"items":{"type":"string"}},"apt":{"anyOf":[{"$ref":"#/$defs/AptSpec"},{"type":"null"}],"default":null},"brew":{"anyOf":[{"$ref":"#/$defs/BrewSpec"},{"type":"null"}],"default":null},"cargo":{"anyOf":[{"$ref":"#/$defs/CargoSpec"},{"type":"null"}],"default":null},"chocolatey":{"type":"array","default":[],"items":{"type":"string"}},"custom":{"type":"array","default":[],"items":{"$ref":"#/$defs/CustomManagerSpec"}},"dnf":{"type":"array","default":[],"items":{"type":"string"}},"flatpak":{"anyOf":[{"$ref":"#/$defs/FlatpakSpec"},{"type":"null"}],"default":null},"go":{"type":"array","default":[],"items":{"type":"string"}},"nix":{"type":"array","default":[],"items":{"type":"string"}},"npm":{"anyOf":[{"$ref":"#/$defs/NpmSpec"},{"type":"null"}],"default":null},"pacman":{"type":"array","default":[],"items":{"type":"string"}},"pipx":{"type":"array","default":[],"items":{"type":"string"}},"pkg":{"type":"array","default":[],"items":{"type":"string"}},"scoop":{"type":"array","default":[],"items":{"type":"string"}},"snap":{"anyOf":[{"$ref":"#/$defs/SnapSpec"},{"type":"null"}],"default":null},"winget":{"type":"array","default":[],"items":{"type":"string"}},"yum":{"type":"array","default":[],"items":{"type":"string"}},"zypper":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false},"ScriptEntry":{"anyOf":[{"type":"string"},{"type":"object","properties":{"continueOnError":{"type":["boolean","null"]},"creates":{"description":"Skip the script if this path already exists. A leading `~` expands to\nthe home directory; a relative path resolves against the script's\nworking directory. Existence follows symlinks.","type":["string","null"]},"idleTimeout":{"description":"Kill the script if it produces no stdout/stderr output for this duration.\nPrevents scripts from silently hanging on unresponsive resources.\nFormat: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.","type":["string","null"]},"interactive":{"description":"Run the script attached to the terminal (inherited stdin/stdout/stderr,\nno spinner, no output capture, no idle timeout) so it can prompt the\nuser — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when\nstdin is not a terminal (CI, piped input, or any daemon-run phase) the\nscript is skipped with a warning rather than hanging on instant EOF.","type":"boolean"},"onlyIf":{"description":"Run the script only if this command exits zero. A non-zero exit skips\nthe script (the condition for running was not met). Evaluated with the\nsame shell, working directory, and environment as the body.","type":["string","null"]},"run":{"type":"string"},"shell":{"description":"Interpreter to use for inline commands. Ignored (and rejected) on file scripts.","$ref":"#/$defs/ScriptShell"},"timeout":{"type":["string","null"]},"unless":{"description":"Run the script only if this command exits NON-zero. A zero exit\n(success) skips the script (the guarded state already holds).\nEvaluated with the same shell, working directory, and environment as\nthe body.","type":["string","null"]},"workdir":{"description":"Working directory for the script. By default every lifecycle script\nruns in the user's home directory — never the config source tree — so\na relative write can't pollute the user's GitOps repo. Set `workdir`\nto override: a leading `~` expands to home and `$VAR`/`${VAR}` expand\nagainst the script environment (which always carries `$CFGD_MODULE_DIR`\nand `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`,\n`workdir: $CFGD_MODULE_DIR`, or an absolute path all work.","type":["string","null"]}},"required":["run"]}]},"ScriptShell":{"description":"Interpreter for inline lifecycle scripts.","oneOf":[{"type":"string","enum":["sh","bash","zsh","pwsh","cmd"]},{"description":"Platform default: `sh` on Unix, `cmd.exe` on Windows.","type":"string","const":"auto"}]},"ScriptSpec":{"type":"object","properties":{"onChange":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}},"onDrift":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}},"postApply":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}},"postReconcile":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}},"preApply":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}},"preReconcile":{"type":"array","default":[],"items":{"$ref":"#/$defs/ScriptEntry"}}},"additionalProperties":false},"SecretSpec":{"type":"object","properties":{"backend":{"type":["string","null"]},"envs":{"type":["array","null"],"items":{"type":"string"}},"source":{"type":"string"},"target":{"type":["string","null"]},"template":{"type":["string","null"]}},"additionalProperties":false,"required":["source"]},"ShellAlias":{"type":"object","properties":{"command":{"type":"string"},"name":{"type":"string"}},"required":["name","command"]},"SnapSpec":{"type":"object","properties":{"classic":{"type":"array","default":[],"items":{"type":"string"}},"packages":{"type":"array","default":[],"items":{"type":"string"}}},"additionalProperties":false}}}
```

'''
