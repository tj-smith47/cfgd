{
  "apiVersion": "cfgd.io/v1alpha1",
  "description": "A reusable unit of packages, files, scripts, and environment.",
  "fields": [
    {
      "children": [
        {
          "description": "",
          "name": "command",
          "required": true,
          "type": "string"
        },
        {
          "description": "",
          "name": "name",
          "required": true,
          "type": "string"
        }
      ],
      "description": "",
      "name": "aliases",
      "required": false,
      "type": "[]object"
    },
    {
      "description": "",
      "name": "depends",
      "required": false,
      "type": "[]string"
    },
    {
      "children": [
        {
          "description": "",
          "name": "name",
          "required": true,
          "type": "string"
        },
        {
          "description": "",
          "name": "value",
          "required": true,
          "type": "string"
        }
      ],
      "description": "",
      "name": "env",
      "required": false,
      "type": "[]object"
    },
    {
      "children": [
        {
          "children": [
            {
              "description": "The encryption backend to use (e.g. \"sops\", \"age\").",
              "name": "backend",
              "required": true,
              "type": "string"
            },
            {
              "description": "When encryption must be enforced. Defaults to `InRepo`.",
              "name": "mode",
              "required": false,
              "type": "string"
            }
          ],
          "description": "Encryption settings for this module file.",
          "name": "encryption",
          "required": false,
          "type": "object"
        },
        {
          "children": [
            {
              "description": "Keys/values to deep-merge into the target, leaving unmentioned keys untouched. Values are literal (no template rendering). Mutually exclusive with `script`.",
              "name": "ensure",
              "required": false,
              "type": "object"
            },
            {
              "description": "File format to parse the target as. Inferred from the target's extension when omitted.",
              "name": "format",
              "required": false,
              "type": "string"
            },
            {
              "description": "A script path or an inline command that receives the target's current content on stdin and writes the new content to stdout. A relative path resolves against the module directory for a module file (`spec.files[]`) and against the config directory for a profile file (`spec.files.managed[]`); a value that resolves to no file is run as an inline command. Mutually exclusive with `ensure`.",
              "name": "script",
              "required": false,
              "type": "string"
            }
          ],
          "description": "Structured merge or script configuration for `strategy: Patch`. Required when `strategy` is `Patch`, rejected otherwise (enforced by `validate_module_file_entries`, not the JSON schema).",
          "name": "patch",
          "required": false,
          "type": "object"
        },
        {
          "description": "Unix permission bits (e.g. \"600\", \"644\") to apply after deployment.",
          "name": "permissions",
          "required": false,
          "type": "string"
        },
        {
          "description": "When true, the source file is local-only: auto-added to .gitignore, silently skipped on machines where it doesn't exist.",
          "name": "private",
          "required": false,
          "type": "boolean"
        },
        {
          "description": "Not required when `strategy` is `Patch`; required otherwise (enforced by `validate_module_file_entries`, not the JSON schema).",
          "name": "source",
          "required": false,
          "type": "string"
        },
        {
          "description": "Per-file deployment strategy override. If None, uses the global default.",
          "name": "strategy",
          "required": false,
          "type": "string"
        },
        {
          "description": "",
          "name": "target",
          "required": true,
          "type": "string"
        }
      ],
      "description": "",
      "name": "files",
      "required": false,
      "type": "[]object"
    },
    {
      "children": [
        {
          "description": "",
          "name": "aliases",
          "required": false,
          "type": "object"
        },
        {
          "description": "Skip the install script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks. Only meaningful for a `prefer: [script]` install; ignored otherwise.",
          "name": "creates",
          "required": false,
          "type": "string"
        },
        {
          "description": "",
          "name": "deny",
          "required": false,
          "type": "[]string"
        },
        {
          "description": "",
          "name": "minVersion",
          "required": false,
          "type": "string"
        },
        {
          "description": "",
          "name": "name",
          "required": false,
          "type": "string"
        },
        {
          "description": "Run the install script only if this command exits zero. A non-zero exit skips the install (the condition for installing was not met). Only meaningful for a `prefer: [script]` install; ignored for manager-backed installs (those are idempotent via the manager's installed-package query).",
          "name": "onlyIf",
          "required": false,
          "type": "string"
        },
        {
          "description": "",
          "name": "platforms",
          "required": false,
          "type": "[]string"
        },
        {
          "description": "",
          "name": "prefer",
          "required": false,
          "type": "[]string"
        },
        {
          "description": "",
          "name": "script",
          "required": false,
          "type": "string"
        },
        {
          "description": "Run the install script only if this command exits NON-zero. A zero exit (success) skips the install (the package already appears present). Only meaningful for a `prefer: [script]` install; ignored otherwise.",
          "name": "unless",
          "required": false,
          "type": "string"
        }
      ],
      "description": "",
      "name": "packages",
      "required": false,
      "type": "[]object"
    },
    {
      "description": "Platform tags gating the whole module. When non-empty and the current platform matches none of them, the module is skipped entirely (it appears as a Skipped action rather than vanishing). Tags are matched against OS / distro / arch via `Platform::matches_any`; the canonical macOS token is `macos`.",
      "name": "platforms",
      "required": false,
      "type": "[]string"
    },
    {
      "children": [
        {
          "description": "",
          "name": "onChange",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        },
        {
          "description": "",
          "name": "onDrift",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        },
        {
          "description": "",
          "name": "postApply",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        },
        {
          "description": "",
          "name": "postReconcile",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        },
        {
          "description": "",
          "name": "preApply",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        },
        {
          "description": "",
          "name": "preReconcile",
          "required": false,
          "type": "[](string | object)",
          "variants": [
            {
              "description": "",
              "name": "string",
              "required": false,
              "type": "string"
            },
            {
              "children": [
                {
                  "description": "",
                  "name": "continueOnError",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Skip the script if this path already exists. A leading `~` expands to the home directory; a relative path resolves against the script's working directory. Existence follows symlinks.",
                  "name": "creates",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Kill the script if it produces no stdout/stderr output for this duration. Prevents scripts from silently hanging on unresponsive resources. Format: \"30s\", \"2m\", etc. If unset, no idle timeout is enforced.",
                  "name": "idleTimeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script attached to the terminal (inherited stdin/stdout/stderr, no spinner, no output capture, no idle timeout) so it can prompt the user — e.g. `echo \"press Enter when done\"; read`. Requires a TTY: when stdin is not a terminal (CI, piped input, or any daemon-run phase) the script is skipped with a warning rather than hanging on instant EOF.",
                  "name": "interactive",
                  "required": false,
                  "type": "boolean"
                },
                {
                  "description": "Run the script only if this command exits zero. A non-zero exit skips the script (the condition for running was not met). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "onlyIf",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "run",
                  "required": true,
                  "type": "string"
                },
                {
                  "description": "Interpreter to use for inline commands. Ignored (and rejected) on file scripts.",
                  "name": "shell",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "",
                  "name": "timeout",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Run the script only if this command exits NON-zero. A zero exit (success) skips the script (the guarded state already holds). Evaluated with the same shell, working directory, and environment as the body.",
                  "name": "unless",
                  "required": false,
                  "type": "string"
                },
                {
                  "description": "Working directory for the script. By default every lifecycle script runs in the user's home directory — never the config source tree — so a relative write can't pollute the user's GitOps repo. Set `workdir` to override: a leading `~` expands to home and `$VAR`/`${VAR}` expand against the script environment (which always carries `$CFGD_MODULE_DIR` and `$CFGD_CONFIG_DIR`), so `workdir: ~/.local/share/app`, `workdir: $CFGD_MODULE_DIR`, or an absolute path all work.",
                  "name": "workdir",
                  "required": false,
                  "type": "string"
                }
              ],
              "description": "",
              "name": "object",
              "required": false,
              "type": "object"
            }
          ]
        }
      ],
      "description": "",
      "name": "scripts",
      "required": false,
      "type": "object"
    },
    {
      "description": "System configurator settings contributed by this module. Deep-merged into the profile system map; module values override profile values at leaf level.",
      "name": "system",
      "required": false,
      "type": "object"
    }
  ],
  "kind": "Module",
  "location": "modules/<name>/module.yaml",
  "name": "Module"
}