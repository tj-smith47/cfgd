Plan
  Config   <CONFIG_DIR>/cfgd.yaml
  Profile  tiny
  ⚠ `--only packages.brwe` matched no actions in this plan; owners present: profile:tiny

⚠ No actions in scope — the active filter excluded all 1 action planned; the system was not reconciled

→ Actions exist in phase: Files — run `cfgd plan` without the filter to preview them
