# cfgd Shared Utilities — `cfgd-core/src/util/`

Cross-cutting functions live in `cfgd-core/src/util/<topic>.rs`, re-exported through `lib.rs` as `cfgd_core::<name>(...)`. **Before writing any helper, check the topic file first**; a function two modules need goes in the topic matching its domain.

This file is an **INDEX**. The reasoning — why a helper exists, what breaks without it, which callers must agree — lives in the item's own rustdoc. Open it before changing a call site.

## Topic files

| Topic file | What goes here |
|---|---|
| `util/constants.rs` | API/CSI strings, k8s label keys, OCI annotations, timeouts, histogram buckets |
| `util/time.rs` | Timestamps + duration parsing |
| `util/yaml_merge.rs` | YAML deep merge + env/alias/string-vec mergers |
| `util/strings.rs` | Env-var / alias parsing + validation, shell/XML/k8s-name escaping |
| `util/paths.rs` | Config/home paths, path validation, dir copying, test-home thread-local |
| `util/fs_perms.rs` | Cross-platform symlinks, permissions, exec-bit, inode/file-index identity |
| `util/file_io.rs` | Atomic writes, file-state capture |
| `util/process.rs` | Command helpers, memo generation counter, tool seams, env reads, shutdown |
| `util/env_session.rs` | User-session live env refresh shell-outs (`launchctl`/`systemctl --user`/`setx`) |
| `util/git.rs` | Git + Sigstore/cosign command factories, repo-reference resolution |
| `util/hashing.rs` | SHA256 helpers + loose-semver parsing/satisfaction |
| `util/apply_lock.rs` | Apply / source / backup lock acquisition |
| `util/reconcile.rs` | `EffectiveReconcile` + per-module reconcile-patch resolution |
| `util/encryption.rs` | sops/age detection |
| `util/config_inputs.rs` | The config-file input recorder |

## Constants

- `API_VERSION` — canonical API version (`cfgd.io/v1alpha1`); never a string literal.
- `CSI_DRIVER_NAME` — canonical CSI driver name (`csi.cfgd.io`).
- `MODULES_ANNOTATION` — canonical annotation key (`cfgd.io/modules`).
- `LABEL_MACHINE_CONFIG` / `LABEL_DEVICE_ID` — k8s label keys; use in gateway/controllers instead of raw strings.
- `OCI_ANNOTATION_PLATFORM` — OCI manifest annotation key; use in `oci.rs` instead of the raw string.
- `PROFILE_SCRIPT_TIMEOUT` (5m) / `COMMAND_TIMEOUT` (2m) / `GIT_NETWORK_TIMEOUT` (5m) — never hardcode the durations.
- `DURATION_BUCKETS_SHORT` / `DURATION_BUCKETS_LONG` — Prometheus histogram bucket presets.

## Time

- `utc_now_iso8601()` — the ONE ISO 8601 timestamp function; never wrap it.
- `unix_secs_now()` / `unix_secs_to_iso8601(secs)` — epoch seconds, and epoch → ISO 8601.
- `iso8601_to_filename_safe(ts)` / `utc_now_filename_safe()` — strip `:-TZ` so a stamp can be a path segment; never inline the `.replace`.
- `BACKUP_TIMESTAMP_FORMAT` / `unix_secs_to_backup_stamp(secs)` / `utc_now_backup_stamp()` — the `spec.backups[]` snapshot stamp (`20260512T143025Z`), the `{timestamp}` `namePattern` variable.
- `parse_duration_str(s)` — `"30s"` / `"5m"` / `"1h"` / plain seconds → `Duration`.
- `humanize_age_since(ts, now)` — an ISO 8601 timestamp's age (`"2h ago"`); `now` is a parameter, never a clock read, so a render pins in a test.
- `is_stale_since(ts, now, threshold_secs)` — whether `ts` is older than the threshold; shares its parse-and-diff primitive with `humanize_age_since` so the two cannot disagree.
- `humanize_until(ts, now)` — the FORWARD twin (`"in 5m"` / `"due now"`); `None` on an instant already past, which a caller words as an age instead. Shares the same signed primitive, so the two cannot disagree about where an hour ends.
- `humanize_age_cell(ts, now)` / `humanize_until_cell(ts, now)` — the ONE rendering of a listing COLUMN over either twin, including its absence: a past column reads `never`, a future one `-`. An unsubtractable stamp falls back to itself, never to the absence word; the ISO 8601 instant stays in `-o json`.
- `humanize_age_magnitude_cell(ts, now)` — the MAGNITUDE twin of `humanize_age_cell`, over the same `span_magnitude` bucketing the backward and forward twins read, so no two cells disagree about where an hour ends. The value states the magnitude when the KEY is the dimension (`Age`: `status`'s Last Apply row, `log`'s and `compliance list`'s column, `compliance snapshot`'s row — `3m`) and the relation when the key names the EVENT (`Last Applied`, `Last Run`, `Next Run`, `Created`, `Last Sync` — `3m ago`); one helper each. `Age  3m ago` restated the pastness its key asserts, the same doubling `short_commit`'s rule forbids under a `Commit` header. The CRD `Age` printcolumns are `type: date`, which `kubectl` renders bare already. `no_age_slot_restates_the_dimension_its_key_names` walks every CLI function for an `Age` key or header and fails one reading `humanize_age_cell`, and its converse; `the_magnitude_cell_is_the_relation_cell_minus_its_relation_word` pins the shared bucketing.
- `humanize_duration_secs(secs)` — an ELAPSED span a person reads (`3600` → `1h`), for a duration that is not an instant and so has no `now` to subtract from (a daemon's uptime). At most two units; the raw seconds stay in `-o json`.

## YAML / merges

- `deep_merge_yaml(base, overlay)` — recursive YAML value merge.
- `fold_env_layer(base, overlay, separator)` — the LAYER fold of `spec.env`, and the ONE place `PATH` concatenates instead of being replaced: every declaration that survives the platform filter contributes its entries around the ambient `$PATH` reference (written once, in the first declaration's spelling), deduplicated by `normalize_path_entry`, leaving exactly one `PATH` entry. Every layer fold reads it (`config::merge_layers`, `composition::merge_with_policy`, `composition::engine`, `reconciler::verify::merge_module_env_aliases`); `merge_env` stays the DOCUMENT-edit semantic the CLI setters need, where the caller is rewriting one YAML file's own list and last-wins is what they typed.
- `union_extend(target, source)` — `Vec<String>` merge without duplicates.
- `merge_env(base, updates)` / `merge_aliases(base, updates)` — merge by name, later wins.
- `split_add_remove(values)` — split `&[String]` into (adds, removes); a leading `-` is a removal.

## CLI parsing / validation

- `parse_env_var(input)` / `parse_alias(input)` — parse `KEY=VALUE` / `name=command`, validating as they go.
- `validate_env_var_user_name(name)` — the one to use for USER input: shell-safety plus the reserved `CFGD_*` refusal.
- `validate_env_var_name(name)` / `validate_alias_name(name)` — low-level shape checks; prefer the user-facing one above for user input.
- `shell_escape_value(value)` — escape a value for a shell `export`.
- `xml_escape(s)` — escape `&<>"'` for XML/plist.
- `escape_control_chars(s)` — render control characters as visible `\xNN`. The ESCAPE policy of the output system's three (see `output/mod.rs`'s `//!`); reach for it on a string a command builds OUTSIDE the renderer.
- `cursor_safe(s)` (`output/mod.rs`) — the ONE renderer FOLD for text cfgd did not author. Its routed-slot inventory and the fold/escape/strip split live in that module's `//!`; `output-module.md` states the rule.
- `PackageListSpec` (`config/profile_spec.rs`) + `ScriptCommand` (`config/module.rs`) — the NAMED map forms of the two list-or-map unions (`pipx: {packages: […]}`, `postApply: {run: …}`), read by both the deserializer and the schema. A union's object arm is always a named type: an inline arm renders as `object` in `cfgd explain`'s type span (`<([]string | object)>`), a word nobody can look up. And a field's rustdoc never restates a type its own span shows — `Accepts a bare list or an \`AptSpec\` mapping.` under `<([]string | AptSpec)>` above a `Variants` section was one fact three times. `every_union_typed_field_renders_its_shapes_once_and_its_fields_by_name` walks every union field of every kind for both.
- `sanitize_k8s_name(name)` — RFC 1123 DNS label sanitization.
- `manager_family(manager)` — the registered manager name's family (`brew-cask` → `brew`), the unit a lane and every exclusion check must agree on. Never where the manager is NAMED rather than serialized.
- `PACKAGE_SCHEMA_PATHS` + `package_schema_path(path)` (`config/resolve.rs`) — the ONE table mapping a `--package` prefix onto its `spec.packages` list; never hand-match manager names.
- `SimpleManager::install_cmd` / `update_cmd` + `manager_install_script(manager, packages)` (`crates/cfgd/src/packages/`) — the declaration table owns HOW a family installs, down to its weak-dependency policy, and the lookup is how a surface OUTSIDE the apply path composes one. Every surface that emits an install for a declared package — the apply path, every `module export` dialect — composes from them; a hand-written install verb in a `format!` is the bug this exists to make unwritable, and it shipped as a `module export` Dockerfile whose `apt-get install -y --no-install-recommends` differed from what the apply that provisioned the same packages ran. `every_manager_install_the_cli_emits_spells_its_weak_dependency_policy_once` walks `crates/cfgd/src` for an install verb outside the table, hatched with `// install-verb-ok:` where the string is advice to a human rather than a command cfgd runs.
- `EntryOwners` (`config/resolve.rs`) + `ProfileLayer::owner_token()` — which layer last wrote each env var / alias, claimed BY the merge; never re-derived by a second walk.
- `EnvOrigins` + `ManagerPathDir` (`reconciler/env_engine.rs`) — the provenance half of a generated env file (each line's trailing `# kind:name`). Fold BOTH sides of a deployed-vs-rendered comparison through `without_owner_comment`. The PATH line's `# manager:brew,cargo` comment is a WIDER vocabulary than `OwnerKind`'s — a comma list no `Owner` name may hold, so `OwnerKind::from_token("manager")` stays `None` on purpose.
- `ModuleSurfaces::of(spec)` + `script_summary()` / `hook_names()` / `script_counts()` / `script_total()` (`modules/surfaces.rs`) — the ONE tally of what a module declares. `ScriptSpec::hooks()` is its ordering authority; never list the six lifecycle hooks by hand.
- `split_module_file_resource_id(id)` (`crates/cfgd/src/cli/live_drift.rs`) — the inverse of `module_file_resource_id`, kept beside its producer so the two spellings cannot drift.
- `DecisionContents::for_decisions(resolved, rows, config_dir, owners)` + `decision_row(item)` (`reconciler/pending.rs`) — the ONE derivation of WHAT a pending source decision would put on the machine AND of the `(outranked by <owner>)` annotation, built once by the caller. Display-only; three consumers, and they must stay three.
- `merged_entry_owners(resolved, modules)` (same file) — the `owners` argument above: the layer merge's own per-entry claims with every resolved module folded on top, in the env engine's order. Never a second opinion about precedence.
- `pending_decisions_title(count, scope)` / `declined_decisions_title(count, scope)` + `DecisionsTitleScope` + `MSG_ANSWER_DECISIONS` / `MSG_INCLUDE_DECLINED_DECISIONS` (same file) — the two decision-section titles (the count is the section's ANNOTATION, never a row of its own; a plan's `not included in this plan` qualifier joins it in the same parenthetical through `DecisionsTitleScope::NotInThisPlan`) and the ONE instruction for closing a decision, one wording per state. A surface listing decisions reads all of them; `every_decisions_section_title_comes_from_the_one_builder` walks both crates for a hand-built literal.
- `nothing_to_do_verdict(pending)` + `MSG_NOTHING_TO_DO` (`reconciler/run.rs`) — the closing line of a run with no actions, `Role` included: `Ok` only when nothing is withheld, `Pending` + `Nothing to apply — N decisions pending` otherwise. `plan` and `apply` both settle through it, so neither can report "up to date" over an unanswered item.
- `declared_decision_fingerprints(spec)` + `DeliveredItems::content_hash_for(resource)` (`reconciler/pending.rs`) and `StateStore::latest_decision_content_hash` / `set_decision_content_hash` (`state/decisions.rs`) — the per-ITEM fingerprint a source decision is keyed on. A `Notify` item is re-asked only when ITS fingerprint differs from the row's; a row with none recorded is backfilled through `SourcePolicyReview.fingerprint_backfill`, never re-asked. Never gate on a whole-source hash.
- `list_or_struct_schema::<T>` / `list_or_packages_vec_schema` (`config/profile_spec.rs`) — the `#[schemars(schema_with)]` twins of the `list_or_struct` / `list_or_packages_vec` deserializers. **A schema declares every shape the deserializer accepts**: a `deserialize_with` that WIDENS a field past its Rust type carries a `schema_with` that says so, because `explain`'s Type column and `Variants` section, `-o json`'s `type`, and the SchemaStore-published `cfgd-profile` / `cfgd-source` schemas all read the reflection and none of them can see serde. Without the pair, the published `BrewSpec` was a bare object rejecting `brew: [ripgrep, fzf]` — the form the rustdoc example used and `cfgd apply` accepted. A NARROWING validator (`deserialize_platform_tags`, `deserialize_module_version`, `deserialize_source_priority`) needs no twin: a valid document still validates. `every_list_or_map_package_field_declares_both_shapes_in_its_schema` walks the source for the two deserializers and the live schema for both shapes. Re-bless through `task schema:bless` and regenerate through `task schema:generate`.
- **A doc comment on a `JsonSchema` type is USER documentation in full.** `schemars` takes every paragraph of the `///` block as the schema `description`, and that string lands on `cfgd explain` (fields and variants), in `explain -o json`, in the SchemaStore-published `tests/golden/schema/*.json` and in every generated agent skill. `PackageListSpec`'s WHY paragraph named two private functions in rustdoc link syntax on twelve drilldowns. The WHY goes in a `//` comment on the type or on the private helper it pairs with, never in the `///` block. `no_schema_description_addresses_a_maintainer_instead_of_a_user` walks every description of the in-process render and of the committed goldens for link syntax, a private helper's name, or maintainer vocabulary.
- `profile_inventory_blocks(resolved)` (`crates/cfgd/src/cli/profile/show.rs`) — a profile's inventory as named `KvPair` blocks; its two consumers differ only in section DEPTH. Never mint a second inventory renderer.
- `source_manifest_doc_sections(doc, manifest, policy, profiles_dir)` (`crates/cfgd/src/cli/source/show.rs`) — the ONE render of a config source's manifest; `policy` is derived by the CALLER, once, from the value the payload carries.
- `load_config_and_profile_module_scoped(cli, printer, module_filter, with_profile)` (`crates/cfgd/src/cli/helpers.rs`) — the `--module` isolate shared by `apply` and `plan`: loads and drains the config exactly once, and never resolves a profile for an isolated run.
- `TokenHits` + `warn_zero_match_tokens(...)` + `known_module_names(config_dir)` (`crates/cfgd/src/cli/plan_ops.rs`) — the SSOT per-token accounting for `--skip`/`--only`, pushed into `Plan::warnings` so one producer feeds the header and `-o json`.
- `RunContext` (`crates/cfgd/src/cli/run_context.rs`) — the per-invocation holder of everything a run builds at most once. Build one at a `cmd_*`'s top; never `Sync`, so concurrent phases get the resolved objects, not the context.
- `ManifestCache` + `resolve_manifest_packages_cached(spec, config_dir, cache)` (`crates/cfgd/src/packages/mod.rs`) — reached through `ctx.resolve_manifest_packages`; never process-global, a manifest being stable only for one run.
- `Platform::current()` (`platform/mod.rs`) — the ONE platform detection per process; `detect()` is for the detection unit tests only.
- `PlatformGated` + `applicable_here(entries, platform)` + `validate_platform_tag(tag)` / `deserialize_platform_tags` (`platform/mod.rs`) — the ONE gating predicate and the ONE list filter over every type carrying a `platforms:` list (`ModuleSpec`, `ModulePackageEntry`, `EnvVar`, `ShellAlias`), plus the serde hook every one of those four fields deserializes through, so a near-miss tag (`darwin`, `osx`, `win`, `arm64`, `AMD64`) is refused at parse time with the spelling cfgd matches. `platform_annotation()` is the display half (`platforms: macos/linux`), read by every surface that shows a gated entry. A gated-off entry is filtered BEFORE the layer fold, never inside it: an entry that does not apply here must not displace one that does.
- `http_agent(timeout)` (`http.rs`) — the ONE `ureq::Agent` per named timeout, so pooled TLS connections survive; a caller needing a different agent SHAPE builds its own.
- `format_bytes(bytes)` — the workspace's ONE human byte-size renderer; never hand-roll a second scale.
- `Theme::PRESET_NAMES` + `Theme::preset(name)` (`output/theme.rs`) — the ONE preset list and lookup; `cli::resolve_theme_config(path, preset)` is the ONE composition of a printer's theme block.
- `Theme::arrow()` / `Printer::arrow()` — the ONE arrow glyph for a rendered `old -> new` relationship; a pure `Doc` builder takes an `arrow: &str` parameter instead. A REVERSED relationship is reworded with "from", never a mirrored glyph.
- `Absence` (`NotInstalled` / `Missing` / `NotFound`) — the ONE absence vocabulary, chosen by WHAT is absent, not how badly. Two consumers turn its literals into WIRE values.
- `drift_kind_label` / `is_shell_drift_kind` / `drift_item_subject` / `drift_operands` / `drift_terse_cause` / `env_file_row_is_redundant` (`output/mod.rs`, beside `drift_detail`) — the drift report's VOCABULARY to `drift_detail`'s RENDERING. All six DISPLAY-only: every stored, hashed or `-o json` string keeps its producer's literal.
- `MergedEnvItems::display_values(kind, id)` (`reconciler/verify.rs`) — supplies that pair for the one kind whose STORED operands are opaque markers. Build the merge once per command; never call it on a value about to be persisted.
- `reconciler::primary_env_file(home)` — the primary managed env file on this host, and the ONE public spelling of that platform split; a consumer minting its own seeds a basename the real verifier never reads.
- `FoldedPath` + `fold_path_line(...)` / `primary_folded_path(...)` (`reconciler/env_engine.rs`) — the ONE `PATH` line a generated env file carries, over BOTH producers (the declared `spec.env` entry and the bootstrapped manager directories) and BOTH readers (the write and the per-item comparison). A dialect renders the parts through `FoldedPath::value(quote, inherited, separator)`; it never decides which entries or in what order, and a second `export PATH=` in one file is the bug this exists to make unwritable.
- `module_status_display(stored, drifted)` + `MODULE_STATUS_INSTALLED` / `MODULE_STATUS_ERROR` (`state/types.rs`) — the ONE derivation of the word a person reads for a module's state. `Drifted` is DERIVED, so a recorded-state surface passes `drifted: false`.
- `ENV_SESSION_RESOURCE_ID` (`state/types.rs`) — the recorded `resource_id` of the live-session env surface; never the bare `"refresh"` literal, which two readers spelled independently.
- `Action::pre_skip_reason()` (`reconciler/types.rs`) — why an action cannot run on THIS host, answered while the plan is read. The ONE seam: `Phase::action_count` excludes what it names and `render_plan_tree` settles it, so the plan's promise and the apply's tally are one number.
- `reconciler::attempted_count(actions)` (`reconciler/types.rs`) — the ONE spelling of that exclusion as a COUNT, over any action iterator. `Phase::action_count` (and through it `Plan::total_actions`) counts a whole plan with it; a caller holding a scoped subtree — `build_plan_output` over `in_scope_tree` — counts the subset it LISTED with the same one. Never hand-roll `.filter(|a| a.pre_skip_reason().is_none()).count()`, and never price a `--phase` payload off the unfiltered plan.
- `ActionResult.not_attempted` + `ApplyResult::not_attempted()` + `RunTally.not_attempted` + `ApplySummary::Actions.not_attempted` — the SAME predicate on the apply side: the reason `pre_skip_reason` gave, recorded on the result the dispatch settles, so a withheld action is neither `succeeded` nor `skipped` (which RAN and changed nothing) and `succeeded + skipped + failed == planned_total` holds. `outcome_clauses(tally)` (`reconciler/run.rs`) is the ONE decomposition of what a run's actions came to: one clause per outcome CLASS, each carrying the role that class's own action rows carry. `outcome_counts` JOINS them for the daemon's single log line (a log line has no glyph column), and `rollup_lines` lays out one clause per LINE, so the `Role::Ok` line states only what succeeded. A withheld count states itself in the withheld role; fused into the tick's sentence, `✓ 20 actions succeeded, 1 not attempted: no session manager` painted work that never happened under a green glyph and invited a reader to sum `20 + 1 + 2` against a header promising `Actions 22 planned`, of which the withheld count is deliberately no part. A settled skip and a pre-skip both paint `Role::Skipped` on their own rows, so their clauses do too — the rollup speaks the tree's vocabulary rather than minting a second one; what no clause may do is share a LINE with another class. The SHORTFALL line (`N actions not attempted`, work the run planned and never reached) is a fifth class with its own line and its own `Role::Info`. The stored summary and `-o json` carry the withheld count as `notAttempted`, outside `total`. `every_outcome_class_in_a_rollup_carries_its_own_role` walks the four classes across every `ApplyStatus` arm plus the shortfall shape, hatching only the abort line (a sentence about an interrupted run, `Role::Warn`, not a count list); `a_pre_skipped_action_is_priced_outside_the_counted_rollup` pins the rollup, `refresh_live_session_reports_no_session_manager_when_unavailable` the real apply.
- `output::Elapsed` (`::row(d)` / `::wall(d)`) + `StatusBuilder::wall_duration(d)` (`output/renderer/status.rs`, `output/status_builder.rs`) — what a duration slot MEASURES beside how long. `.duration(d)` is a row's own span (` (23.8s)`); `wall_duration` is the closing rollup's wall-clock total (` (278.2s wall)`), and `render_run_rollup` is its one caller. Lanes run concurrently, so rows may sum to more than the total; the word is the explanation. `normalize_snapshot_durations` keeps it (` (XXs wall)`). The ROW slot measures what RAN, not what SUCCEEDED: `Role::Fail` and a `continueOnError` `Role::Warn` are timed exactly like `Role::Ok`, the failure arm having settled `duration: None` while a failed `apt-get install rustc` unpacked a whole dependency closure — the one untimed row in its phase, and the whole of the gap between the rollup's `(N.Ns wall)` and the sum of the rows above it. `apply::failed_action_ran` is the ONE exclusion and names the two failures that ran NOTHING: a `Refuse` node IS the refusal, and a dependent the coordinator swept was never dispatched. It asks the action and the error rather than trusting the `Duration::ZERO` a sweep hands over, which the floor would print as `(<0.1s)` and so state as a measurement. A DECLARED noop stays untimed on the success side for the same reason. `a_failed_action_that_ran_is_timed_like_a_successful_one` pins both halves. The wall total hangs off the rollup's FIRST line — the one that NAMES the run — never off whichever line came last: attached to the last, a `Partial` block closed on `2 actions failed (274.0s wall)`, which reads as the two failures having burned four and a half minutes. And every line of a rollup block reserves the glyph column, because a rollup is a block of STATUS lines: the `Partial` arm's failure count took `Role::Accent`, which claims no glyph by design, so the one line counting the failures hung a column left of the two above it — unmarked, in a report where every failed action row carries a red `✗`. `Role::Accent` / `Role::Secondary` stay iconless for an INLINE span or a deliberate non-verdict label (`AccentHeading`'s `Caveats`, `status`'s scan-age line above the drift rows); neither is a rollup line. `the_rollups_elapsed_hangs_off_the_line_that_names_the_run` and `every_rollup_line_reserves_the_glyph_column` (which walks every `ApplyStatus` × every `RunTitle`, both a met and a short tally) pin the pair.
- `SystemContext::next_step(message)` / `NoteSink::next_step(printer, message)` (`providers/mod.rs`) — an INSTRUCTION from a configurator, routed as `ActionNote::next_step` under a collecting sink and `Printer::hint` standalone. `report(role, …)` is for what the run has to say about itself: `Warn` when the reader must act on a degraded outcome, `Info` for a side report, never `Ok`. `every_provider_note_takes_its_role_from_whether_the_reader_must_act` walks the population.
- `packages::shared::brew_caveat_asks_the_reader_to_act(body)` — whether a brew `==> Caveats` body is an instruction (`Role::Warn`) or a "here is where it went" report (`Role::Info`); read by `extract_caveats` for the brew family only, every other manager's caveat being a line the tool itself labelled a warning. The note's SUBJECT is settled one layer up: `ActionNote::attributed_to(subject)`, called by `reconciler::apply::collect_caveats`, re-tags a TAGGED note with the display subject of the action that produced it, the one place both are in scope. `extract_caveats` sees only the manager name and its output, so `[brew]` was the most any producer could say — and the closing section groups by OWNER, so the action line naming what brew spoke about is gone by then; the section's body-dedup then dropped the second formula's identical `Bash completion has been installed to` outright. One slot, filled with what produced the note — never a second tag vocabulary, and never on an UNTAGGED note, whose owner group already identifies it. `every_caveat_names_the_subject_that_produced_it` settles two actions of one manager under one owner with the same body and fails on a line lost or a bare `[brew]`.
- `backup::outcome_role(clean, produced_artifact)` + `outcome_detail(error, size)` + `snapshot_subject(destination)` / `restore_subject(target, snapshot)` / `rollback_subject(target, copy)` (`backup/mod.rs`) — the three-way role, the one detail slot and the three SUBJECTS every completed backup-engine outcome settles as. `report_backup_record`, `report_restore` and `report_rollback` are the three mutating verbs of one command and read all three slots from here, so a fourth outcome cannot be worded twice and the rows cannot split grammars (`snapshot <name>` / `restore <target> from <name>` / `rollback <target> from <copy>`, all lowercase verb heads). The target is IN the restore subject, never a `Destination` kv row under it: the row is the action, and its object is where the data went. The error leads the detail; the size joins it in parentheses.
- `backup::safety_copy_hint(safety, name)` (`backup/mod.rs`) — the ONE closing hint a restore or a rollback leaves after displacing live data: where the previous contents went (`SidecarOutcome::detail`, so a reused copy never reads as one written this time) and how to put them back. `report_restore` and `report_rollback` both read it; never a second literal beside either.
- **The grammar split** (`.claude/rules/output-module.md`, "The grammar split, stated once") — a run BODY row is lowercase imperative, a RESULT line is sentence case past tense, a provider NOTE is a sentence or the command as run. Not a helper but a rule with two pinning walks that PARTITION the sources: `every_action_row_subject_opens_on_a_lowercase_verb` takes every subject slot under `reconciler/` and `backup/`, `every_result_line_is_sentence_case` takes every other role, hint and spinner finish in BOTH crates, and the latter asserts the two sides sum to the whole. Judged by neither, `init`'s twelve `Role::Ok` rows read as past-tense reports six lines above the reconciler's imperatives with nothing to say which was wrong. A row that NAMES rather than reports (a tool's own lowercase name, an init system's verb) carries `// name-row-ok: <why>` on the line or the line above.
- `reconciler::action_produced_detail(action, installed, delivered, versions)` + `env_write_summary` / `deploy_files_summary` / `installed_packages_summary` / `provisioned_managers_summary` (`reconciler/apply.rs`) — the fact an action PRODUCES, worded for the detail slot of its own row (`write ~/.cfgd.env — 3 vars, 3 aliases`, `deploy init.lua — 5 already deployed`, `sys install npm, jq — 1 already installed`, `provision cargo, npm via apt — 1 of 2 managers`). A shortfall on a set the subject names in full (files, packages) states the COMPLEMENT, never a ratio: `— 7 of 9 packages` puts two numbers over one set on one row, and the reading available to a reader pairing them is not the one the run means. The managers arm keeps its ratio because a provision subject names every member and its shortfall is a failure, not a set already in place; `no_produced_detail_restates_a_total_the_subject_already_gives` renders every complement arm at full AND short counts. `installed` is the count the EXECUTOR re-read off the machine, carried out of `apply_package_action` on `ActionRun::installed` the way `changed` is carried out for the role, and `None` for a preview, which has not run yet; the subject stays the planned set on both trees, so the detail is the only seam that can say the run landed fewer. The ONE producer both trees read: `render_plan_tree` hangs it off the bullet (`SectionGuard::bullet_detail`), the apply's settle off the status row, and `build_plan_output` carries it as `PlanActionOutput.detail`. A subject builder in `reconciler/format.rs` never parenthesises a count into its text: `every_produced_count_is_an_action_rows_detail` walks that file's literals for a `({} <noun>)` / `({} of {} <noun>)` trailer, hatched with `// name-row-ok:` where a parenthetical names rather than counts. A new produced count adds an arm here, never a `format!` at a call site. **A detail never restates a total the subject already gives**: a subject names every operand it acts on, so the names ARE the total and a full deploy states no count (`deploy a, b, c, d, e, f — 6 files` said six twice); `no_produced_detail_restates_a_total_the_subject_already_gives` renders every complement arm over five operands and fails a detail opening on the operand total. A `ManagerAction::Provision` states the same fact in the same grammar: a node promises an AVAILABLE manager, so an earlier node or the `Prerequisites` phase may already have delivered one it names, and its count is the executor's own re-read of `is_available` — the same re-read that settles `changed`, so a node that ran nothing takes `Role::Skipped` rather than a green tick over a count it did not earn. Landing everything it named states no count, its subject already naming every manager, which is the rule both sibling arms hold for their own subjects. The `ActionResult.installed` filter reads every producer, so a count `-o json` carries is one the report also states. `every_manager_node_states_what_it_produced` binds every `ManagerAction` variant with no `..`; `a_provision_whose_manager_was_already_delivered_states_the_count_that_says_so` drives the real apply. `versions` is the second fact the same re-read yields, over the same carrier (`ActionRun::versions`): the VERSION each manager the node itself landed reports through `PackageManager::tool_version` (`packages::shared::tool_version_from` + `parse_tool_version`, one `--version` spawn, `None` on any failure), read only for a member `pending` held — a manager that was here already produced nothing this row can claim. A single-manager node states it bare (`provision brew via installer — 4.6.3`), a batch beside each name (`cargo 1.89.0, npm 11.4.2`), a shortfall keeps its count and parenthesises what landed (`1 of 2 managers (npm 11.4.2)`); `-o json` carries it as `versions`. `RefreshIndex`, `Prerequisite` and `Refuse` are hatched in the walk with their reasons — a prerequisite's tool is a means the run needed, not a product it delivered, and its subject already spends the detail grammar on `required by`. `a_landed_provision_states_the_version_it_delivered` drives the real apply through `apply_manager_plan_at(Verbosity::Normal)`, the one fixture verbosity at which a settled row's detail is rendered. A package shortfall is worded from PROVENANCE, never from the count alone: `delivered` is how many of the planned set THIS run's own `Prerequisites` nodes landed (`Reconciler::provisioned_packages`, recorded at the provision's settle through `managers::provision_delivered_packages` — the ONE derivation of what a node delivers, read by the plan's own elision too, so the two cannot disagree — and judged by `plan::delivered_by_this_run` on installer FAMILY and package name), so `brew install node, jq — 1 already installed, 1 provisioned by this run` tells the reader which of the two skipped rows was their machine and which was cfgd's own earlier row; `— 2 already installed` over a node cfgd had just bootstrapped claimed a pre-existing state the run itself produced. Both clauses may appear, in that order, each only when non-zero. `widest_produced_detail(action)` is the PRICING twin — the widest the detail can come to over every split of the planned set — read by `report_trailing_allowance` alone, so the column claim is priced over a real count rather than a preview's `None`. `a_shortfall_this_runs_provisions_delivered_is_worded_as_delivered` pins the wording, the pricing and the family fold; `a_package_a_prerequisite_landed_is_not_installed_again_by_the_packages_phase` drives the real apply.
- `reconciler::link_deployed_digest(source)` + `Reconciler::refresh_link_deployed_hashes(fm, resolved, modules)` (`reconciler/files.rs`) — the ONE digest of what a converged link entry DEPLOYS (a file's own sha256; for a directory link, the fold of every regular file under it, symlinks skipped as the deploy skips them), read by BOTH halves of the recorded-hash refresh: the profile-level `spec.files.managed` row (`CfgdFileManager::link_deployed_content`) and a module's aggregate row (`module_link_deployed_rows`). A module's aggregate covers every file its `files:` entries deploy, never only the entries that happen to be file-shaped — the row id's declared-entry count is a promise about coverage, and a directory skipped on one side is an aggregate that can never move, so the sync tick that carried an edit under `files/lua` read like an idle one. `None` on any unreadable file abandons the whole digest rather than recording a partial reading. `a_module_deploying_a_directory_by_symlink_reports_the_file_that_moved_inside_it` and `link_deployed_content_covers_every_file_under_a_directory_entry` pin the two halves.
- `ApplyStatus::{as_str, display_str, human_str}` (`state/types.rs`) — the stored / `-o json` / human spellings of an apply's outcome, pinned as a wire contract.
- `ApplySummary` + `::to_column()` / `::prose(stored)` (`state/types.rs`) — the ONE typed shape of the `applies.summary` column and the ONE prose rendering every human surface reads it back through. Never build the column with `json!`, and never print the stored string. **A slot names its outcome only when that outcome OCCURRED**, one rule for every clause: the lead count (`succeeded` / `restored`) is the single deliberate exception, so an all-failed run still opens on a number. `0 failed` beside a `Result Success` row is a clause that cannot carry information — the status already forbids the non-zero — and the LIVE rollup for the same counts (`outcome_clauses`) never emitted it, so one run read two ways depending on whether it was live or recalled. `-o json` drops the zeros too (`skip_serializing_if`), which left the human row the only surface printing them. `no_summary_slot_names_an_outcome_that_did_not_occur` walks every slot of both variants with its count zeroed in turn.
- `cli::apply::refresh_link_deployed_hashes(reconciler, resolved, modules)` (`crates/cfgd/src/cli/apply.rs`) + `Reconciler::file_manager()` — the ONE post-apply settle of the hashes an apply just recorded. `record_managed_resources` writes every row with no hash and `refresh_managed_resource_hash` counts NULL → value as a move, so a verb that records rows also settles their content hashes before it returns, or the first daemon tick after it backfills them and its `N deployed files refreshed` reports work the machine never did. Four members: `apply` (converged and applied arms), `init --apply`, `module create --apply`, and the daemon's own applying tick (which holds its file manager apart from the registry and so calls the core `Reconciler::refresh_link_deployed_hashes` directly, AFTER the apply — the pre-apply refresh at the top of the tick cannot see rows the apply has not written yet). `init --apply` and `module create --apply` were the two that skipped it. `every_plan_running_verb_settles_its_link_deployed_hashes` walks every `ApplyRun::new` in the CLI and the tick and fails one that executes without reaching the seam; a preview hatches with `// no-hash-refresh-ok: <why>`. `init_apply_settles_the_hash_of_every_link_deployed_row_before_it_returns` pins the real bootstrap, and the daemon's `a_tick_that_refreshed_a_deployed_file_says_so_instead_of_reading_idle` seeds the hash a real apply records so the clause depends on the bytes MOVING, never on a NULL backfill.
- `ApplyResult::{succeeded, skipped, failed}` + `RunTally.skipped` (`reconciler/`) — a successful action that CHANGED nothing is skipped, not done. Every count comes from these; `!failed` is not a success count.
- `ManagerAction::Provision { declared, .. }` + `DeclaredProvision` (`reconciler/types.rs`, minted by `managers::declared_manager_routes`) — the module's own route to a tool cfgd is about to bootstrap as a MANAGER. A resolved `spec.packages` entry naming the same canonical tool is the more specific statement, so the provision installs that entry's resolved name through its `prefer`-chosen installer and the `Packages` phase then elides it through `package_survives_elision`; the entry's `minVersion` floor comes along for free, `modules::resolve_package` having already applied it. `None` keeps the manager's own `bootstrap_plan_given` cascade, priced against the managers this run will DELIVER, not only the ones the host already has: `plan_managers` hands the cascade a `delivered` predicate answered from its own `graph.members` (a member settled `Present` or `Provision` counts), so npm plans `via brew` behind the brew node the same plan provisions instead of falling to the apt arm a host-only probe answered — the 534-package `provision npm via apt` the README hero recorded. `PackageManagerExt::bootstrap_plan()` is the host-only view (`delivered` always false) for every caller outside a plan being built (`doctor`, the selector vocabulary, recorded PATH dirs). Every detector in `packages/shared` (`detect_brew_system_method`, `detect_brew_or_system_method`, `detect_system_method`, `detect_go_bootstrap_method`, `detect_system_arm`) asks `delivered` BEFORE the host on every arm; `bootstrap` asks the execute-time question (`brew_available()` in `bootstrap_brew_arm`), against a host the provision ahead of it has already changed and whose memo it invalidated. `a_cascade_prefers_the_mediator_this_run_provisions` / `a_cascade_with_no_brew_in_the_plan_keeps_its_host_arm` pin the planner (the closure never pulls brew IN for a cascade's sake), `every_manager_with_a_brew_arm_plans_via_the_brew_this_run_delivers` walks every real manager declaring a brew arm (`mediated_packages("brew")`). A declared route records no bootstrap and folds no `creates_path_dirs`: apt's `rustc` does not create `~/.cargo/bin`, and the installer's real directories are recorded by `record_created_path_dirs` anyway. A route is minted only for an entry whose AUTHOR named the manager — `ResolvedPackage::manager_declared`, set at `modules::resolve_package` where the `prefer` list and the `aliases` map are in scope and never re-derived by a second walk. `ResolvedPackage::manager` is what RESOLUTION chose, so a bare `- name: npm` read as a declaration and rendered `provision npm via apt` over apt's whole node toolchain, in place of the brew cascade `plan_managers` documents npm as preferring. With no route the cascade provisions the tool through a DIFFERENT manager, which the entry's own manager cannot see, so the coherence half is `packages::provisioned_this_run(mcx.provisioned, pkg)` — the elision keys on the canonical tool the provision DELIVERED, not on the manager that delivered it. `Reconciler::provisioned` is the mirror of `Reconciler::unprovisioned`: recorded from a manager node's own success in `settle_action`, snapshotted onto `LaneRun` as the phase opens, and never re-probed (`is_available()` cannot tell a tool this run installed from one that was here all along, and only the former makes the entry beside it a duplicate). `a_tool_a_module_declares_is_provisioned_by_the_modules_own_route` pins that a DECLARED tool is installed exactly once and by the manager the module named; `a_resolver_defaulted_manager_is_not_a_declared_provision_route` / `an_author_declared_manager_still_routes_the_provision` pin both sides of the predicate; `a_tool_this_run_provisioned_is_not_installed_again_by_a_module_entry` pins the coherence half; `resolve_package_records_whether_the_author_named_the_manager` pins the provenance at its producer. The PLAN-time half of that coherence is `Reconciler::elide_provisioned_tools` (`reconciler/plan.rs`): once `plan_managers` has settled which managers this plan bootstraps, a bare module entry (`manager_declared: false`) naming a tool one of those provisions delivers is judged by the same `package_survives_elision` over the provision list and dropped, so one run never carries both `provision npm via brew` and `apt install …, npm`. An entry naming a PACKAGE the provision itself installs is dropped too, `manager_declared` notwithstanding — the module's own route (`provision pipx via brew` IS that entry's `brew install pipx`) and the cascade's mediated package (`provision npm via brew` IS a `brew install node`) alike — judged by `delivered_by_this_run` over `managers::provision_delivered_packages`, the ONE derivation of what a provision node delivers and what the apply's settle records into `provisioned_packages`, so the plan and the apply cannot disagree about which package a row above already landed; the hero's `brew install neovim, fd, zoxide, node, pipx, go, stylua, sops, age — 2 provisioned by this run` named two tools on two rows and priced an install the run never performed. The routes are derived ONCE, before anything is elided, and both `plan_managers_with_routes` passes read that one answer: an elision drops the very entries the routes were minted from, so a second derivation off the survivors would hand back a cascade where the module had named a route. The re-plan's MEMBERSHIP is held open the same way: membership starts from what the surviving work names and a cascade's arm is priced off the managers already settled, so eliding a mediator's last consuming entry retired that mediator, dropped the cascade to its host arm, and left the elided package delivered by nothing at all — `elide_provisioned_tools` answers `Some(relied_on)` — BOTH ends of every route it acted on, the installer the pair came through AND the `provisioned_managers()` of the node that delivered it — and `plan_managers_with_routes` seeds them into `wanted_managers` as `extra_wanted`; a manager whose consumers genuinely vanished still retires. Naming only the installer left the DELIVERING node free to retire when the elision also took its last consumer (a bare entry naming the mediator, dropped by the provisioned arm), so the package was delivered by nothing again and the mediator was provisioned for nobody. The delivered arm asks BOTH of the entry's names through `delivering_installer` (the `Option<&str>` view `delivered_by_this_run` is the `is_some()` of): a declared route carries the module's own alias, while a cascade delivers the MEDIATOR's literal (`mediated_packages("brew") == ["node"]`), which an entry writing `aliases: {brew: nodejs}` never spells, so the canonical name the alias resolves to is asked too. The PLANNED `via` is what a node ran — `PackageContext::planned_method` is binding on every cascade, a lost mediator failing the provision instead of substituting an arm — so a recorded pair can never credit the run with a package another route installed; `every_multi_arm_bootstrap_honours_the_planned_method` (`cli/tests.rs`) walks every `fn bootstrap` under `packages/` and fails one that neither routes through an arm-selecting helper (`bootstrap_brew_arm` / `bootstrap_via_brew_then_system` / `bootstrap_via_system_manager`) nor consults `planned_method` in an arm of its own, hatched with `// bootstrap-arm-ok: <why>` where a single-arm bootstrap has nothing to diverge to. The own-arm half is INVERTED, never an allowlist of spawn wrappers: anything past the last helper call that is not a closer or an `Ok(())` counts, so the next wrapper written is judged the day it is written (`run_pkg_cmd`, `run_pkg_cmd_msg`, `run_pkg_query` and a raw `Command` all leaked past the three names the first draft enumerated). `the_bootstrap_arm_walk_catches_an_own_arm_through_any_spawn_wrapper` drives the judgement negatively over every wrapper spelling. `a_tool_this_plan_provisions_is_not_planned_again_by_a_module_entry` pins it on the planned tree, `a_declared_route_entry_this_plan_provisions_is_not_planned_again_on_the_packages_row` the two delivered-package shapes, the aliased spelling and the route that survives its own elision, `a_cascade_the_elision_relied_on_survives_the_re_plan` the membership the second pass must keep, `a_provisions_delivered_pair_is_read_off_the_planned_route` the derivation itself, and `a_tool_this_plan_provisions_is_named_once_in_the_json_payload` (`cli/plan_ops/tests.rs`) the same claim on the serialized payload rather than at its producer.
- `PackageExec::installed_now(pm, cx)` (`reconciler/packages.rs`) + `Reconciler::package_survives_elision` (`reconciler/plan.rs`) — the EXECUTE-time re-read of what a manager has, and the ONE predicate every elision judges by. A plan is priced before the `Prerequisites` phase runs, and that phase installs packages (`apt install npm pipx` provisions two managers and lands two apt packages a module may declare itself), so an install action re-reads the machine before it runs and installs only what is still missing; an action left with nothing to do settles `changed: false` — a skip that RAN — and so marks no module changed and re-runs no postApply hook. Never compare against the names a provision mentioned: the truth is one memoized listing away, and the memo is already voided by the generation every install moves. Fail-OPEN on a manager that cannot be queried, exactly as the planner's own elision does. `a_package_a_prerequisite_landed_is_not_installed_again_by_the_packages_phase` pins both halves. The node's post-install VERIFICATION words itself from what the run did, never from the post-condition it is asserting: a declared route ran no cascade — it is an ordinary install by another manager — so its failure names the installer and the package (`cargo not on PATH after apt installed rustc`) and never calls itself a bootstrap, and a real cascade's failure names the method it ran (`still not on PATH after the {via} bootstrap`). `PackageError::BootstrapFailed` is the one variant whose sentence its CALLERS build rather than the command that failed, which is why it is the one that can be built with no operand at all; `every_bootstrap_failure_names_what_it_installed` walks both crates' construction sites and fails a `format!` whose every placeholder is the manager's own name, `a_declared_routes_verification_failure_names_the_package_it_installed` pins the real refusal. The sibling plan fact — the `available_version` a package row quotes — is NOT re-read: `action_display_subject` is one string across the preview bullet, the alignment column and the executed row, so `ManagerAction::RefreshIndex` invalidates the resolution generation instead, and no offer taken before a refresh is answered to any caller after it. `Action::pre_skip_reason` cannot go stale in this direction at all: `SYSTEM_INSTALLABLE_TOOLS` is `curl` alone and no manager's `mediated_packages` names `setx`/`launchctl`/`systemctl`, so nothing the `Prerequisites` phase can install makes a session manager appear.
- `PackageExec::refuse_withheld_manager(name)` + `usable_manager(name)` + `withholding_managers(list)` + `Reconciler::unprovisioned` + `ManagerAction::managers_left_unavailable()` — a manager a node of THIS run already failed to provision is unavailable for the rest of it, answered from the failed node and never re-probed. `is_available()` bottoms out in a path lookup whose memo the intervening installs moved and whose last arm is a bare `exists()`, so a cargo cfgd had just reported it could not provision answered available one phase later and `cargo install stylua` was spawned into `No such file or directory (os error 2)` instead of `PackageError::ManagerNotAvailable`'s recovery sentence. Both package shapes that name a manager ask — `PackageAction::Install`/`Uninstall` through `usable_manager` (the run's verdict, THEN availability) and `ModuleActionKind::InstallPackages` through `refuse_withheld_manager` alone, because merely-unavailable is deliberately still dispatched there: the action runs alone with the phase draining around it, and the drain may be what delivers the manager (`unavailable_manager_action_drains_the_phase`). Asked before the manager is touched, so neither the install nor the `installed_for` re-read behind it spawns. Within one phase the lane dispatcher's `fail_dependents` withholds the downstream work; this is the same withholding carried ACROSS phases, where no DAG edge reaches, so `LaneRun::unprovisioned` is a SNAPSHOT taken as the phase opens rather than a live view. A `Prerequisite` node names nothing when it fails — `apt install curl` failing says nothing about apt. `a_package_action_for_a_manager_whose_provision_failed_is_never_spawned` pins both shapes.
- `PackageSchemaPath::noun()` + `DEFAULT_PACKAGE_NOUN` (`config/resolve.rs`), reached from the CLI through `PackageRef::noun[_capitalized]` — the word a confirmation line calls a `--package` entry (`tap` / `cask` / `package`), read off the schema path so the add verb and the remove verb cannot disagree.
- `ActionNote::next_step(message)` (`providers/mod.rs`) — a caveat that is an INSTRUCTION rather than a report. Renders as a hint, after every report in its group; `warn` / `info` stay for what the run has to say about itself.
- `note_tag_doubling_error(tag, message)` (`providers/mod.rs`) — the ONE statement of the rule that a note's body never repeats its own tag. `ActionNote::body` composes `[{tag}] {message}` AROUND the tag, so a body opening on it prints one fact twice (`⚠ [npm] npm has no writable global prefix`); judged on the OPENING word only, a tag named mid-sentence being information rather than a stutter. The twin of `system_key_doubling_error` for the other name-prefixed composition. `npm_warn_parts` / `strip_caveat_self_tag` hold the rule for CAPTURED bodies; this holds it for AUTHORED ones, through a `debug_assert` in `NoteSink::report_tagged` (which sees a bound tag too) and through `no_provider_note_repeats_its_own_tag`, which walks the literal call sites `provider_note_calls` reads for both note rules.
- `reconciler::render_caveats(printer, groups)` (`reconciler/apply.rs`) — the ONE render of the closing `Caveats` section, over BOTH callers (`cli::plan_ops::print_caveats`, and the per-configurator snapshot bridge with its single group). A caveat is deduplicated by MESSAGE across the WHOLE report, in BOTH slots (the hint half and the report half), the FIRST occurrence keeping it — the message, never the composed body, because `collect_caveats` re-tags every note with the subject of the action that produced it, so no two TAGGED notes on a real run ever compare equal and a body-keyed fold could only fire in a test that assembled its groups by hand; a group left holding nothing but repeats opens no heading. A caveat states a fact about the MACHINE, so a run that provisions a manager in `Prerequisites` and uses it again in `Packages` files one fact under two owners — `[brew] Bash completion has been installed to …` printed twice in one eight-line section, reading as two installs to one path. A render fold only: the `-o json` payload keeps every note under its own owner. `a_caveat_message_renders_once_per_report` pins the cross-owner case and the group that opens anyway; `every_caveat_slot_dedupes_by_message` walks both slots plus the within-one-group shape the bridge caller is the only producer of. All three drive `collect_caveats` rather than a hand-built group, so the attribution stands in the fold's way exactly as it does on a run.
- `cli::status::recorded_module_tallies(items, declared)` → `ModuleTally { packages, files, scripts }` — what the Managed Resources table says this host manages per module, one slot per module-owned kind the Type column spells (`env` is cfgd's own), and the ONE source of the Modules headline's counts. A headline derived from resolution disagrees with the table under it; a kind with no slot vanishes from the headline while its rows stay in the table, which is why the tally is a named struct and why `every_module_owned_kind_the_table_lists_has_a_slot_in_the_headline` walks `display_type`'s own arms. A recorded `script` row carries no count of its own, so its number comes from the `declared` entry the table cell reads.
- **Aliases lead env vars on every surface naming the pair** — the two halves of the shell surface carry no header saying which is which, so a surface listing them the other way makes a reader who learned the order on one screen read the next one wrong. The population: `status <module>`'s compact `Shell` breakdown and both itemized views, `module show`'s two sections, the pre-approval review a remote module is read on (`print_module_review_summary`, whose env half is named `Environment`), `ModuleStatus`'s own field order, and `diff`'s `Shell` section — which needs no arm of its own, its findings being sorted by recorded kind and `alias` preceding `env` there for the same reason. `every_surface_naming_the_shell_pair_lists_aliases_first` (`crates/cfgd/src/cli/module/tests.rs`) renders all five and fails on a surface that inverts them.
- **`status <module>`'s header leads on its two themed rows** — `Status`, then `Scope`, then `Last Applied` and the counts: the two rows a reader scans for a module's standing sit together at the head rather than with a recorded age between them. `the_status_and_scope_rows_lead_a_module_report` pins the order.
- `cli::status::managed_resource_rows(items, modules, profile)` + `owner_render_order(rows)` (`crates/cfgd/src/cli/status.rs`) — the Managed Resources table's rows, and the ONE place its Owner column is decided and ordered. The vocabulary is `reconciler::owner_of`'s, so the dashboard names a resource exactly as the plan tree, the apply tree and `diff` name the very actions that recorded it: an `env` row (`reconciler::ENV_RESOURCE_TYPE`) is CFGD's own and carries the group suffix the tree heads it with — `cfgd:session` for the one row whose id is `ENV_SESSION_RESOURCE_ID`, `cfgd:env` for the generated env file and the rc source line, both named by `reconciler::SESSION_GROUP` / `ENV_GROUP` — a `module:` row is its module, and EVERYTHING else the branch reaches — a package, a managed file, a profile script, a system setting, a secret — is the PROFILE's. `cfgd  brew: gum` two commands after the apply that installed it printed `profile:base / brew install gum` said cfgd had installed it on its own account, and a bare `cfgd` beside the tree's `cfgd:env` was the same divergence one column over. The ORDER is the tree's as well: `owner_from_token` reads each rendered token back into an `Owner` and `owner_render_order` asks `Owner::order` — the ONE way to order owners outside `Phase::from_actions`, which the audit's `Owner ordering (one comparator)` check enforces by refusing a second `sort_key()` application — so the table runs profile, then cfgd's groups in causal order, then the modules, with the lexical row order left as the within-owner tiebreak. A lexical sort alone put `module:` above `profile:` because `m` precedes `p`. `profile` is `Option<&str>`: a `--module` run resolves no profile (`RunContext::config_and_profile` answers a name or an error only for a whole-config run), and a profile-declared row then names no owner at all — `owner_from_token` reads `None` back as `cfgd_core::ABSENT` (`-`), which `owner_render_order` leaves out of the comparator's order so the caller's rank puts it after every named owner, module rows included. `a_profile_declared_row_carries_the_owner_the_apply_tree_heads_its_group_with` builds its expectation from the reconciler's own `Owner` constructors put in order by `Owner::order`, so both the spelling and the column's order fail it on any divergence from the tree; `a_row_no_profile_can_be_named_for_reads_a_dash_and_sorts_last` pins the `None` cell and its sort position. Display only: `status -o json` carries the raw `ManagedResource` rows, which hold no owner at all, and `status <module>` renders no Owner column.
- `Owner::label()` / `Owner::token()` (`reconciler/types.rs`) — the ONE composition of an owner's `kind:name`, as the renderer's tri-colour `OwnerLabel` and as its plain string; `token()` is `label().plain()`, so a heading, a table cell and a serialized token cannot come out as three spellings. Every surface holding an `Owner` asks it (`reconciler/apply.rs`'s group headings, `live_tree`, `run`, `cli/diff.rs`, `cli/status.rs`'s Owner column and its module/source status rows) instead of rebuilding `OwnerLabel::new(owner.kind.as_str(), owner.name.as_str())`, which was the same expression in five files. `OwnerLabel::new(kind_literal, name)` stays the composer for a call site that holds no `Owner` — a heading naming a source or a backup by name. `every_owner_label_of_a_held_owner_comes_from_owner_label` walks both crates for an `OwnerLabel::new` whose argument reads a `.kind` field, `Owner::label`'s own body excepted.
- **A row's VERDICT leads its detail; its counts are the parenthetical** — `✓ module:nvim — Synced (24 packages, 6 files, 7 scripts)`, never `24 packages, 6 files, 7 scripts, Synced`, which is one comma list for two kinds of fact and parses the health word as a fourth inventory item. It also buries the one word a reader scans a column of modules for (`Failed`, `Drifted`) behind three numbers a healthy module carries too. Every other `*_status_display` consumer already gives the word its own slot — a `KvPair::role_valued("Status", …)` row (`status <module>`, `module show`, `source show`) or a table cell (`module list`, `backup list`) — where the key or the header says it is a verdict; a detail slot has no header, so the ORDER has to. `no_status_detail_trails_a_verdict_word_behind_its_counts` renders every stored token `module_status_display` answers over AND walks both crates for a `.detail(format!` literal whose last placeholder follows a comma and names a verdict.
- `ApplyRun::unplanned(ctx, actions)` + `RunContext::subject` / `RunContext::unit_source` (`reconciler/run.rs`) and `backup::RESTORE_ACTION_COUNT` — the plan-less run: a command whose BODY it renders itself still takes the skeleton's header and rollup, and `actions` is the one number both ends state. `subject` is the title's value half (`Restore: notes`), never a row; `unit_source` is the `Source` header row a run over ONE `spec.backups[]` unit carries (`backup run <name>` and `backup restore`, symmetric), the unit's declared path beside `Config`/`Profile`. A run over every unit has no one source and prints no row. Never a synthesized empty `Plan`, and never a second `Config`/`Profile`/`Sources` renderer.
- `reconciler::pseudo_phase(printer, label)` / `sole_phase(printer)` (`reconciler/run.rs`) — the two ways work outside a `Plan` opens its owner groups: under a `Phase: <label>` row where other phases sit beside it (`apply`'s `Backups`, the daemon's `Drift Hooks`), or at the run's own depth when the run has no other phase (`backup run`, the scheduled fire). `ApplyRun::render_backups` picks by whether the run carries a `Plan`; never open a `Phase:` row that only restates the run's title.
- `RunTitle` + `::as_str()` (`reconciler/run.rs`) — what a run calls itself: the heading it prints AND the noun its rollup lines are built from, so `Restore` cannot head a block whose closing line says `Backup`. A new run kind adds a variant here, never a literal at a call site.
- `backup::report_restore(printer, outcome)` — the ONE render of a restore's rows and rollup, returning the `RunTally` its caller reports. Never re-word a restore's outcome beside it.
- `reconciler::report_align_width(plan, filter, budget)` + `report_trailing_allowance(plan, filter, budget)` + `Printer::report_column_beside(width, trailing)` / `report_column(width)` + `Printer::live_column_for(width)` — ONE alignment column per REPORT, not per phase, claimed only if the widest thing a row may print BESIDE it (a lane wait reason, a produced count) still fits on this terminal. The allowance is measured over rows a trailing CAN land on, through the dispatcher's own vocabulary: `lanes::phase_wait_reasons` words every `Hold::Edge` a Manager node's `depends_on` can raise and every `Hold::Lane` two actions sharing `action_lane` (the `via` family for a provision, the manager family otherwise) can raise, per PHASE, and `widest_produced_detail` prices each produced count; the two are joined by the widest per phase, never summed over the report. Priced over EVERY action holding a manager, a lone `apt install` nobody queues behind carried a `queued behind` it could never print, and on the hero shape at 128 columns that phantom (`70 + 2 + 69 > 124`) retreated the column the wait reasons actually printed would have fit under. The claim, the allowance and the budget are ONE computation: `Printer::subject_budget_floor` is the action-row line minus the glyph minus `WAIT_FRAMING_WIDTH` (` — queued behind `, pinned against `wait_reason` by `the_subject_budget_reserves_exactly_the_wait_framing`), halved between the subject and the row it waits on; `reconciler::report_subject_budget(plan, filter, printer)` WIDENS that floor to what the line leaves after the glyph and THIS report's own `report_trailing_allowance` (re-priced once at the wider budget, kept only if the claim still fits), and `report_column_beside(budget, width, trailing)` holds it with the column so `Printer::subject_budget` answers the claimed budget to every reader inside the claim — the preview, the settled script row (`module_script_subject_within` / `script_run_subject_within`), the live tree and the wait lines. An operand list is never cut, so a subject may be WIDER than the budget; that row wraps, and `report_align_width` is priced over each subject's FIRST physical row — which is the whole subject exactly when it fits the budget — so no operand list can widen the claim or withdraw it from the rows that do fit. `report_trailing_allowance` leaves out a trailing wider than the budget for the same reason: its own row wraps and glues. `group_column` still NARROWS the claim to what the trailing fits beside rather than dropping it to 0. Priced over every subject alike, one 77-column `postApply:` row withdrew the column from the hero's twenty other rows at 125 columns while the pin passed at 128 with one column of slack. `the_reports_column_is_claimed_and_its_subjects_bounded_at_every_width` walks 80..=200 including 125 over `hero_plan`, asserting a claim that is never 0, one x for every row HEAD within it, and every operand of every row on screen. `a_wait_reason_is_priced_only_over_rows_it_can_name` pins the predicate; `a_report_wide_enough_for_its_wait_reasons_keeps_its_one_column` renders the hero-shaped plan on an emulated 128-column screen and fails a retreated column, a cut wait reason, or a wrapped row whose detail does not close its own last physical row — the live twin of `group_trailing_allowance`, judged once at the claim, because a live group's rows arrive one at a time and `live_group_column` can see only the glyph: on a 44-column terminal a padded `brew install gum` pushed `queued behind provision brew via homebrew` off the line. A live painter (`PhaseTree`) pads to `live_column_for(width)` — the claim when one is held — so a repaint and the commit that replaces it pad to one column, and a row too wide for it WRAPS on both: `LiveRow::set_status` lays its line out through the same `wrap::wrap_body_with_trailer` the permanent line takes (`Renderer::compose_status_split`, so the duration is one composition), and `LiveRow::window` wraps a running row's label before the spinner's own `clamp_label` sees it — indicatif rewinds the rows a message DECLARES, so an explicit `\n` costs nothing while a hard wrap it cannot see is what strands a row. The one column cut left of text cfgd AUTHORED is `condense_script_label_within`'s truncation arm, through `wrap::clamp_at_token`, which once took `chars().take(n)` and cut the hero's `postApply` row at `markdown-preview.nvim/a…`, one column past the `/`; a table cell keeps the column `clamp`, a bare spinner's `clamp_label` holds its own single row, and `window::push_line` cuts FOREIGN captured bytes, all hatched with `// plain-clamp-ok: <why>` — the retreat is to the last space, comma or `/` within sixteen columns so the marker follows a whole token. `every_column_cut_of_cfgd_authored_text_retreats_to_a_token` drives both cutters one column past each boundary and walks `output/` and `reconciler/format.rs` for an unhatched cut idiom. `a_wait_row_on_a_narrow_terminal_retreats_its_column_and_wraps_its_reason` pins the retreat and the wrap over both hold wordings on an emulated 44-column screen, and `a_running_row_names_every_operand_at_eighty_columns` the in-flight label. the width is measured over every action the run will print (`in_scope_tree`, `PhaseCoverage::Rendered`), claimed by whoever can see the whole report, and released when the guard drops. `SectionGuard::live_column` prefers a claimed report column over its own, so a pseudo-phase (backups, `cfgd:env`) lands on the same x position as the plan's phases. A caller measuring one phase makes the detail column jump mid-report. The claim binds every DETAIL-BEARING row shape, not just status rows: a produced count renders as a bullet (`- write ~/.cfgd.env — 3 vars, 3 aliases`), and `Emitting::bullet_column` answers it the same three ways `route_status` answers a status row, `- ` being exactly as wide as a glyph and its space. `every_detail_bearing_row_of_a_report_lands_in_the_reports_one_column` renders one action through both trees and fails on a byte of divergence.
- `output::renderer::{action_subject_style, action_detail_is_muted}` — the ONE emphasis mapping for an action row's two halves, read by all three painters (`Printer::action_status`, `SectionGuard::action_status`, `LiveRow::set_action_status`) plus `emit_action_line`. A WITHHELD role (`Pending` / `Skipped`) holds its subject back and lets the reason speak: subject muted, detail bright. Never decide either half at a call site — that is how one row inverted between the plan tree and the apply tree.
- `reconciler::lanes::wait_reason(Hold)` + `lanes::Hold::{Edge, Source, Lane}` + `live_tree::Wait { subject, reason }` — a blocked row's two SLOTS, and the ONE wait-line sentence per HOLD kind, whose verb names the RELATION, never the mechanism: `waiting on <row>` for a hold whose occupant produces what the waiter needs — an edge or tier barrier the row has not cleared (`Edge`), or its family's hoisted source registration (`Source`: the formula may only exist in the repository the tap adds, a dependency expressed as lane precedence rather than a DAG edge, and named `waiting on` whether the tap is running or merely hoisted ahead) — and `queued behind <row>` only for a lane turn behind unrelated work (`Lane`: `brew-cask install firefox` behind `brew install neovim`). Reading the mechanism had `brew install gum — queued behind brew-tap install charmbracelet/tap` deny the one dependency the tap exists to satisfy. `every_hold_kind_carries_its_own_wording` binds every `Hold` variant without a wildcard; `a_row_held_by_its_familys_source_registration_says_waiting_on` pins both halves (`lanes_busy` and `source_held`) beside the unrelated lane turn, and `an_edge_blocked_node_names_the_last_thing_that_has_to_finish` the edge. It is a REASON, so it fills the row's DETAIL slot beside the blocked action's own display subject. Fused into one string (`<subject> · <reason>`) it was one slot, and the withholding emphasis above mutes the subject and brightens the detail — so the half a reader scans for rendered muted, behind a ` · ` glue standing where the renderer's own ` — ` belonged. A GROUP line has no subject to hold back (its heading is already the head) and carries the reason in the subject slot; an empty subject beside a detail renders as a leading em-dash. `a_wait_rows_reason_is_the_bright_half_of_its_row` paints through the live painter in a colour-carrying theme (`Printer::for_test_with_live_bars_themed`, the one capture that reaches `LiveRow::set_action_status`), every other wait-line test reading ANSI-stripped text and so unable to see it.
- **Owner-carrying composers** (`output/`) — `Printer::heading_owner_prefixed`, `section_owner[_or_collapse]`, `SectionGuard`'s pair, `Doc::section_owner` / `subsection_owner`. Reach for the one matching the call site's shape instead of a hand-built `format!("{kind}:{name}")`; cataloged in `output-module.md`.
- `Printer::command_list(pairs)` and its `SectionGuard` / `Doc` / `SectionBuilder` counterparts — a "command — description" list, for any two-column list whose left side NAMES a thing; never for an ordinary key/value fact. Cataloged in `output-module.md`.
- `CommandPair::typed(key, type_span, value)` / `KvPair::{annotated, nested, role_valued, owner_valued}` / `TitleLabel::typed` (`output/component.rs`) — the renderer-owned styling and layout slots. A caller never paints or indents one itself; cataloged in `output-module.md`. `owner_valued(key, owners)` is the ONE kv slot an owner token may occupy — the tri-colour `OwnerLabel` coat, `, `-joined for the multi-owner scope an isolated run records — and `cli::status::scope_row` is its one caller. `an_owner_valued_row_paints_its_value_through_owner_label` pins the coat; `a_scope_row_carries_the_owner_tokens_it_names` pins the derivation.
- `KvPair::linked(key, text, url)` (`output/component.rs`) + `output::terminal_supports_hyperlinks()` + `config::docs_url(path, version)` (`config/modeline.rs`) — a kv VALUE that is a LINK, its capability gate and the ONE derivation of the URL behind a `docs/` pointer. The renderer owns the split: `text` where the theme carries hyperlinks (`Theme::with_hyperlinks`, stamped by the two PRODUCTION `Printer` constructors alone, from the terminal's own identification and never a flag), the URL ITSELF everywhere else — a repo-relative path is something no terminal auto-links and no reader can paste into a browser, so a fallback to `text` would print a pointer nobody can follow. Colour gates it: `with_hyperlinks(true)` on a colourless theme stays false, an OSC 8 escape being an escape like any other, and every capture leaves it off so no golden picks one up from the developer's terminal. `docs_url` pins the RELEASE tag (`v<cfgd version>`, exactly as `schema_modeline` does and for the same reason), so the page a row opens documents the schema the binary explained. `docs_row(path, url)` (`crates/cfgd/src/cli/explain/mod.rs`) is the ONE composer of the `Docs` row, read by a kind page and a field drilldown alike; `ResourceSchema::docs_url` / `field_docs_url` are its two release-pinned URL derivations (a kind's own pointer, and a field's `spec.<path>` heading falling back to the kind's). `every_docs_pointer_the_cli_renders_goes_through_the_linked_slot` walks `crates/cfgd/src` for a `Docs` row built any other way.
- `Doc::paragraph(text)` (`output/doc.rs`) — a prose paragraph for what a documentation surface says ABOUT the heading above it; cataloged in `output-module.md`.
- `AccentHeading` (`output/accent_heading.rs`, `pub(super)`) — the ONE composer for the "Caveats" heading; deliberately not a `PhaseLabel`, which would render `Phase: Caveats`.
- `Printer::narrate(running, |sp| work)` / `Printer::narrate_silent(...)` — the settle-safe spinner wrappers; never hand-roll guard + spinner + finish. Which one a wait takes is decided by who else SAYS the failure. Cataloged in `output-module.md`.
- `pluralize(count, noun)` / `plural_noun(count, noun)` / `agreeing_verb(count, verb)` — the ONE agreement rendering for a counted sentence. Regular English only; an irregular plural or `be`/`have` is spelled out at the call site. **The noun names the unit the count is IN, and a count of what MOVED is never the coverage of the record that moved.** A row that aggregates many of the noun is not one of it (`1 deployed file refreshed` over a six-entry tree), and an aggregate that moved by a byte says nothing about how many of them did (`52 deployed files refreshed` for a one-line edit under a 52-file module tree — the row's whole coverage printed as movement). `refresh_link_deployed_hashes` answers `RefreshedHashes { rows, files: Option<usize> }`, the count taken entry by entry against the per-file breakdown each `managed_resources` row keeps (`file_hashes`, migration 19; `HashRefresh::Backfilled` stores it silently on a row that predates the column) and `None` when a moved row had none to count against — the tick then names no number rather than the ceiling. A slot that can only state coverage states a SUBJECT, not a number. **And a completion clause describes the MACHINE, never cfgd's bookkeeping about it**: what the tick refreshed was its own record, so `nothing to do, N deployed files refreshed` named a record update as work and contradicted the verdict beside it; the clause reads `N deployed files changed upstream, already live through their links` — what the reader can see on the machine, and why nothing needed doing. `every_counted_clause_names_the_unit_it_counts` walks every `pluralize(` in `daemon/reconcile.rs` and fails a binding/noun pair it has not classified, and walks every folded `{sentence}, …` clause for a bookkeeping verb; `a_one_file_edit_inside_a_module_tree_is_counted_as_one` pins the count over a real tree.
- `sentence_case(word)` — capitalize the first character and leave the rest alone, for a stored lowercase token opening a rendered line (a decision's tier). Never a title-caser: it touches one character, so an acronym or an already-cased word survives.
- `yes_no(Option<bool>)` — the ONE `yes` / `no` / `-` rendering of a tri-state fact in a table cell. `None` reads "not known", never "no".
- `ABSENT` (`util/strings.rs`) — the ONE token a table cell renders for a fact nothing recorded (`-`): what `yes_no(None)` and `humanize_until_cell(None)` answer, and what every hand-filled cell spells. One spelling is what lets `Table::without_unfillable_columns` judge a column with one predicate. A PAST instant's absence reads `never` (`humanize_age_cell`) and is a fact, not an absence.
- `Table::without_unfillable_columns()` (`output/renderer/table.rs`) — drop every column whose every cell is `ABSENT`; every `Table` the CLI emits settles through it, and `every_listing_the_cli_renders_drops_a_column_no_row_can_fill` walks the population. A table with no rows keeps its headers.
- `last_sync_display(last_fetched, now)` + `SOURCES_SECTION` (`crates/cfgd/src/cli/source/list.rs`) — the ONE human rendering of a config source's last fetch (`humanize_age_since`, falling back to the stamp itself, `never` when there is none) and the ONE noun for the section listing sources. Every surface showing either — `source list`, `source show`, `status`, `daemon status`, `doctor` — reads from here; the `-o json` payload keeps the ISO 8601 instant.
- `sources_table(entries, wide, now)` + `configured_source_entries(cfg, state)` (same file) — the ONE `Sources` table and the ONE derivation of the declared catalog it renders. A surface holding live facts (`daemon status`) merges them OVER a catalog row rather than building a narrower table of its own, so no two surfaces name the same source with different columns. `both_sources_surfaces_render_through_the_one_table_builder` fails on a hand-built one. The builder DROPS a column no row in the render can fill (`Drift` off the daemon, `Version`/`Commit`/`Signed` before anything recorded them) instead of padding it with `-`; the `-o json` payload keeps every field. `a_column_no_row_can_fill_is_dropped_from_the_listing` walks every column of a render. Every `SourceListEntry` slot but `name` and `status` is an `Option`: a row is not always a declared `spec.sources[]` entry (the daemon's implicit `local` layer declares no origin, priority or signing demand), and a substituted `0` / `false` reads as a declared fact. `every_catalog_sourced_sources_column_can_be_absent` walks the struct and `daemon_source_row`'s merge.
- `daemon::SourceStatus.drift_count` + `DaemonStatusResponse.drift_count` — a per-source row states facts about ITS source; the machine-wide outstanding drift count is a HEADER fact and is stated once. `current_drift_count(store)` answers for the whole machine, and four sites wrote its answer into `st.sources.first_mut().drift_count` as well, so `daemon status` printed one number twice and the `Sources` table's `Drift` column credited the whole machine's drift to whichever source sat first in the vec — the repo's own golden pinned the OPPOSITE attribution, which is why nothing caught it. Per-source drift is NOT derivable from what is stored: drift is keyed on a resource, and the declaring layer is not on the row — four of the seven action kinds hardcode `LOCAL_LAYER` as their origin and the file watcher records with none at all. So the field is an `Option` nothing fills, `sources_table` drops the `Drift` column through `without_unfillable_columns`, and `SourceStatus` carries no per-source `last_reconcile` at all (a reconcile is machine-wide; the field only ever held the profile-wide stamp). A write that legitimately targets one source finds it BY NAME. `no_daemon_state_write_reaches_a_source_row_by_position` walks `daemon/`'s production files for a positional mutable reach, hatched with `// positional-source-ok:`; `daemon_state_drift_count_stays_machine_wide` pins the state half.
- `daemon::reconcile_tick`'s outcome sentence (`daemon/reconcile.rs`) — the ONE line a tick leaves on the daemon's log, and the only thing distinguishing one tick from the next. `refresh_link_deployed_hashes`'s count is folded into it at ONE seam, beside the per-module name fold, so every arm of the drift branch reports it on the same terms: a pull that lands another machine's edit through a symlink plans nothing, so `reconcile: complete — nothing to do` was byte-identical to the four idle ticks above it and the beat where the sync landed read as cfgd having done nothing. Every other `if let Err` in the file throws away an `Ok(())` or a row id; `every_error_only_arm_of_the_reconcile_tick_is_classified` holds the table a new one is classified in, so the next count-returning call cannot slip in as an error-only arm, and `a_tick_that_refreshed_a_deployed_file_says_so_instead_of_reading_idle` pins the sentence.
- `output::config_profile_rows(config_path, profile)` (`output/component.rs`) — the `Config` / `Profile` header rows, for every surface that reports on a run under a resolved configuration: `ApplyRun::header`, `cfgd status`, `cfgd sync`, `cfgd diff` (both arms), `cfgd profile show` (the Config half; its heading names the profile) and `cfgd daemon status`, which named neither until `DaemonStatusResponse` grew `config_path` / `profile` off `PreLoopSetup::profile_name`. Either half is `Option`, so a surface holding only one asks for only one; the path folds through `PathDisplayExt::display_posix`, so no caller can render a host-native separator into the row. Never a second Config/Profile row builder, and never for a `Profile` naming a DIFFERENT fact — the profile a `spec.sources[]` entry subscribes to is a field of the source, hatched with `// header-row-ok:`. `every_config_and_profile_header_row_comes_from_the_one_builder` walks both crates' production sources for a hand-built row.
- `output::modules_header_row(names, skips)` + `HeaderModule::of_resolved(modules)` + `modules_header_row_for(modules)` (`output/component.rs`) — the SIBLING builder of `config_profile_rows`, and the ONE derivation of what it names: the `Modules` row naming what the resolved profile puts on this machine, directly under `Profile` and ahead of every surface-specific row (`PID`, `Trigger`). The same population reads it — `ApplyRun::header`, `cfgd status`, `cfgd diff` (both arms), `cfgd sync`, `cfgd daemon status` — and only the run header had it, so the README demo opened on a `cfgd status` naming a profile and nothing it resolved to two commands above an apply header that named `nvim`. The LIST is the RESOLVED one, never `merged.modules`: `resolve_dependency_order` expands transitive `depends` and returns the set dependency-first, so the declared list differs in MEMBERSHIP and ORDER, and a profile of one module that depends on another read `editor` on three surfaces and `core, editor` on two. `of_resolved` is that derivation, and `modules_header_row_for` the row over it; every surface but the run header reads the pair, the run header keeping the primitive because its skips come from the plan's own `Skip` actions, which `Reconciler::plan` builds from the very `platform_skip_reason` `of_resolved` carries. `skips` is the platform-gated modules: a skipped name leaves the value and returns in the annotation (`nvim skipped: <reason>`), which is the whole render of `PhaseName::Modules`. `None` when a profile resolves to nothing, which renders no row. `diff` (full) emits its header AFTER `resolve_desired_state`, and so does `sync`, from the PRE-pull resolution it holds when the header prints — the row belongs under `Profile` at the header's own indent, describing the configuration the run started from exactly as `Config` and `Profile` beside it do; emitted instead beside the closing hint it left the key column, read as a result of the sync, and split one header into two blocks. The body reports what the pull changed, and the plan the hint invites reads the new set. `daemon status` reads `DaemonStatusResponse.modules`, a `Vec<HeaderModule>` the reconcile tick writes from its own resolution (so it is absent until the first tick, and a per-module tick — which resolved a subset — writes nothing); `PreLoopSetup` resolves no modules, being documented network-free. `cfgd profile show` is exempt — its body IS the module inventory. A row naming a COUNT of modules, a cache directory, or a set this host is not resolving from its own profile (`kubectl cfgd`'s pod injection) is hatched `// modules-row-ok: <why>`. `every_resolved_profile_header_names_its_modules_through_the_one_builder` walks both crates for a hand-built row, skipping by the CALL (`.section(`, `.heading(`, `section_owner`) rather than by the word; `status_header_names_the_profiles_modules_like_the_apply_header` drives `cmd_status` and `cmd_diff` over one on-disk profile whose `editor` module `depends` on `core` and whose `off-host` module is platform-gated, so a declared list or a dropped skip fails it; `every_surface_reporting_a_resolved_profile_names_the_same_modules` adds `sync` and the daemon render over the same fixture, and both read the row through a helper that asserts it shares the `Profile` row's indent, a comparison of the words alone being blind to a row that left the key column; `a_reconcile_tick_puts_the_profiles_resolved_modules_on_the_status_wire` drives the real tick over that fixture and reads `st.modules` back, so the daemon leg is pinned at its producer rather than hand-fed; `status_json_carries_the_skip_reason_its_modules_row_prints` pins `ModuleStatusEntry.platformSkipReason`, the same fact the human row's annotation states, absent for a module that applies here.
- `daemon::SourceStatus.last_commit` — the commit a source's checkout is at, seeded from the repository HEAD when the daemon starts (`build_initial_source_status` for the cache checkouts, `PreLoopSetup::local_head_commit` for the config dir) and moved by every pull `handle_sync` accepts. `daemon_source_row` prefers it over the catalog's recorded commit; the one `sources_table` shortens it and `-o json` keeps the full id.
- `source_failure_next_step(err, name)` + `subscription_knob_label(key)` (`crates/cfgd/src/cli/source/mod.rs`) — what a reader DOES about a refused source, per error kind, and the rendered label for a subscription knob's wire key. Both display-only: the stored token and the `-o json` field keep the wire spelling.
- `daemon::PullOutcome` + `PullFailure` / `PullFailureKind` + `git_pull_sync(dir)` / `is_git_repository(dir)` / `pull_failure_summary(msg)` (`cfgd-core/src/daemon/git.rs`) + `cli::local_pull_next_step(failure, command)` + `MSG_NOT_A_REPOSITORY` (`crates/cfgd/src/cli/mod.rs`) + `sync::sync_refused(payload)` (`crates/cfgd/src/cli/sync.rs`) — the ONE classification of a local pull, and what each of its four outcomes costs. All THREE callers of the operation read the enum — `cfgd pull`, `cfgd sync`'s `Local Repo` leg and the daemon's own auto-pull leg (`daemon::handle_sync`), which runs on a timer and so put `sync: pull failed` on the journal every tick for a config directory nobody had put under version control — so none of them can call a directory under no version control a failure (`NotARepository` is a verdict, `MSG_NOT_A_REPOSITORY`, exit 0) nor disagree about a real refusal's exit code; a `.git` that is a gitlink FILE (a worktree, a submodule) is a repository like any other, the probe asking whether the entry EXISTS. `pull_failure_summary` is the DISPLAY fold of libgit2's `; class=…; code=…` tail off a result line — the stored `error` and `-o json` keep the full message — and `local_pull_next_step` is the local-layer twin of `source_failure_next_step`, naming the verb that just ran and branching on the PRODUCER's own vocabulary rather than a prefix parsed back out of a message: `git_pull` raises a `PullFailure` carrying the `PullFailureKind` of the step that refused, and the composer matches it with no wildcard, so a step added to the pull does not compile until its fix is written. Four kinds have advice of their own (a missing remote, a diverged branch, an unreachable one, a repository with no first commit); the rest share the arm whose only honest advice IS the general one. `every_local_pull_refusal_names_the_fix_for_its_own_kind` walks `PullFailureKind::ALL`, `every_pull_failure_kind_is_listed_in_all` walks the enum's own source against that array, and `a_sync_tick_over_a_plain_directory_logs_no_pull_failure` drives three daemon ticks over a plain directory. `sync_refused` is the ONE predicate deciding whether `cfgd sync` leaves a nonzero exit: a pull that refused withholds the success verdict (`⚠ Sync incomplete`, worded by `sync_verdict`) because `✓ Synced` two lines under `⚠ Pull failed` claimed the very thing the row above it denied, the same rule `drift_section` and `nothing_to_do_verdict` hold for a verdict over an unverified fact; a source the reader DECLINED at the permission prompt is an answered question rather than a refusal and keeps exit 0, so the predicate reads the payload's own rows rather than the verdict's role. `cmd_sync` is the thin exit wrapper over `run_sync`, exactly as `cmd_apply` is over `run_apply`, so a test drives the refusing fixtures without the process leaving under it. `a_local_pull_failure_exits_1_from_both_verbs_that_pull` pins the exit-code agreement at the subprocess level, `a_gitlink_config_dir_still_gets_its_pull` the gitlink probe, and `sync_local_pull_failure_withholds_the_synced_verdict` the withheld verdict.
- `cli::output_types::SourceOutcome` (`synced` / `updated` / `skipped` / `cancelled` / `failed` / `error`) + `::refused()` / `::declined()` — the ONE vocabulary for the per-source state `cfgd sync` and `cfgd source update` put on the wire, and the ONE reading of which states are a refusal and which are an answered decline. Two commands counting the same outcomes off bare string compares is how one of them ends up crediting a declined source to the failure count; the enum keeps every existing token and shares the two predicates.
- `SourceManager::set_announce_cache_skips(bool)` (`sources/mod.rs`) + `RunContext::fetching_sources()` / `announce_cache_skips()` (`crates/cfgd/src/cli/run_context.rs`) — whether a cache-freshness skip RESTATES itself as it is recorded. The two advisories it governs both tell the reader to run `cfgd sync`, so the command that IS the fetch holds them back — three lines above the section doing the fetching they are noise — while every other thing the composition has to say (a constraint violation, a conflict, the `allowScripts` disclosure) still reaches the operator. Never a blanket `at_verbosity(Quiet)` over the resolution, which swallows the disclosure telling an operator that third-party code will run on their machine. `a_sync_resolving_its_sources_still_discloses_the_scripts_they_carry` pins both halves.
- `success_next_step(Mutation)` (`crates/cfgd/src/cli/mod.rs`) — the SUCCESS-side twin of `source_failure_next_step`: the ONE hint every MUTATING verb closes on, across all five families — `source` (`add`, `update`, `remove`, `replace`, `override`, `priority`), `module` (`create`, `update`, `edit`, `add`, `upgrade`, `registry add`, `keys generate`, `keys rotate`, `push`, `pull`, `build`), `profile` (`create`, `update`, `edit`, `switch`), `secret` (`init`, `encrypt`, `edit`) and `rollback`. The population is every family that WRITES, not the two that were noticed first: `profile update` closed on `√ 3 changes written` and the prompt — the one composition verb `Mutation::ModuleCreated`'s own hint routes the reader INTO — while its exact twin `module edit` was already covered. A `requireSignedCommits` edit points at `cfgd sync`; every composition edit at `MSG_RUN_APPLY`; a verb that produced an ARTIFACT somebody else consumes names the consumer (`push` → a Module resource through `kubectl apply`, with `--apply` as next time's alternative; `build` → `module push`; `pull` → `module show` then `apply`); a verb that produced something nothing references yet names what references it (`profile create` → `profile switch`, `secret init` → `secret encrypt`, `secret encrypt` → `profile update --secret`); and `rollback` — the ONE mutation that moves the MACHINE away from the config rather than the config towards the machine — names `cfgd diff`, never `MSG_RUN_APPLY`, which would undo what it just did. A hint never RE-SPELLS the verb that just ran with concrete arguments: `push`'s once recomposed the invocation from its directory and artifact alone, dropping `--sign --key`, `--platform` and `--attest`, so following it re-pushed a fresh digest the signature did not cover — a re-run hint is a `<placeholder>` template or names a different command. Nor does a hint interpolate a reference the CLIENT resolved (`localhost:5001/…`) into an instruction the CLUSTER carries out; `Mutation::ModulePushed` carries neither the directory nor the artifact, so the shape is unrepresentable. Nor does it name a FILE the verb that just ran CONSUMES, in any spelling its placeholder substitutes to: `push`'s `kubectl apply -f <module>.yaml` substituted to `module.yaml` — the verb's own required input, `kind: Module` carrying the pushed module's `metadata.name`, and on screen two lines above — so following the hint replaced the Module with one holding no `ociArtifact` and no signature, undoing the push and the signing the rows above had just reported; the placeholder is `<module-resource>.yaml`, a name the verb reads nothing from. Every backticked span is nonetheless COMPLETE as printed: dropping the file left a bare `kubectl apply`, which exits 1 on `must specify one of -f and -k` without contacting a cluster, three lines above the operator hand-typing the `-f` the hint had dropped. A qualifier attaches to the NOUN it describes, never to the verb: registration happens at the API server, and what points at the cluster's registry address is the Module. A verb nested inside another's report (`replace` running `add`/`remove`) passes `closing: false` and leaves the hint to the outer verdict. `every_mutating_verb_closes_on_a_next_step` walks all five families — a table per family, each with its own hatch column — and hatches a workflow-ending verb (`profile delete`, `profile migrate`, `secret decrypt`, `module delete`, `registry remove`/`rename`, `export`) with the reason; `every_composed_next_step_names_a_command` walks every `Mutation` shape, each beside the verbs that produce it, and fails a backticked command opening on one of them without a placeholder, naming a file `required_input_files` says the verb requires, failing to round-trip through `Cli::try_parse_from` when it opens on `cfgd`, or — when it opens on a FOREIGN tool — missing the argument that tool refuses to run without or being absent from `foreign_command_is_complete`'s table; `every_source_verdict_counts_iff_its_verb_takes_many_subjects` pins the sibling rule that the verdict carries a count exactly when the verb's clap arity allows several subjects.
- `reconciler::run_next_step(tally, title)` (`reconciler/run.rs`) — what a reader DOES about a run that did not converge, the failure-side twin of `nothing_to_do_verdict` and the ONE next step every non-`Success` verdict closes on (`Partial`, `Failed`, `InProgress`, `Aborted`, and the `Success` arm that attempted nothing). `render_run_rollup` emits it as a `→` hint under the counts, before anything the phases have left to say — the env-file reminder is about a different subject and never substitutes for it. One wording per state; the command is a PLACEHOLDER read off `RunTitle` (`cfgd apply`, `cfgd backup restore <name>`), never the invocation that just ran re-spelled with its own arguments. A verdict LINE states facts only: `Aborted`'s detail says `no partial writes` and leaves `rerun to converge` to the hint, so an instruction cannot hide where a sweep over the hint composers will not find it. `every_unfinished_verdict_closes_on_the_one_next_step` walks every `ApplyStatus` × `RunTitle` pair.
- `heal_drift_hint(module)` + `perform_preview_hint(&PreviewScope)` + `MSG_RUN_APPLY` (`crates/cfgd/src/cli/mod.rs`) — the THREE next-step wordings a non-mutating surface closes on, split by what the reader has just seen. `MSG_RUN_APPLY` invites a preview of changes they have NOT seen (a mutating verb just edited the config, or `sync` just fetched one). `heal_drift_hint` follows a report that FOUND drift, scoped as the report was. `perform_preview_hint` follows the PREVIEW itself — `plan`, `apply --dry-run`, `init --apply --dry-run` — so it neither re-offers the preview that just ran nor claims a drift check was made; it is emitted from `report_plan_verdict`'s has-work arm, the one verdict in the CLI that used to end on a bare count. `PreviewScope` carries the flags the preview was narrowed by, and `PhaseArg`'s `Display` renders them back through clap's own vocabulary (the retired `env` spelling as `prerequisites`), so a bare `cfgd apply` can never be offered after a filtered preview and the composed command re-parses through `Cli::try_parse_from`. `every_verdict_that_shows_pending_work_names_the_command_that_settles_it` walks every state `report_plan_verdict` can close in and fails a work-leaving state that names no command.
- `answer_decisions_hint(pending)` (`reconciler/pending.rs`) — `MSG_ANSWER_DECISIONS` with the bulk form folded in, for a surface that knows the count. The bulk half appears only above one item, where `--all` does something naming the resource cannot. Rendered by exactly two composers, each closing the decisions SECTION from inside: `ApplyRun::render_withheld` (live) and `build_pending_decisions_table_section` (buffered); never a `Doc::hint` after the section.
- `build_pending_decisions_table_section(section, decisions, contents)` (`crates/cfgd/src/cli/source/helpers.rs`) — the ONE buffered render of a decisions section (`decide`, `status`): the owner-grouped rows, then the answer hint at the section's own depth. `every_decisions_hint_closes_its_section_from_inside` walks the emitters.
- `head_signature_accepted(name, repo_dir)` (`sources/mod.rs`) — whether the checked-out HEAD of a source carries a signature cfgd would ACCEPT, read through `verify_head_signature`'s own classifier so display and verification cannot disagree. `None` is "cannot say", which is why the column renders `-` rather than `no`.
- `action_display_subject(action)` (`reconciler/format.rs`) — the ONE display derivation of an action's subject; a preview, an alignment column and an executed line must be one string. Never persist a `DisplaySubject`. A subject NAMES EVERY OPERAND its action carries, in full and at every width — every list-bearing shape (a module's package segments, its file targets, a profile's own install and uninstall) names every package and every path, and a list too long for the line WRAPS under the row's own hang indent (`wrap::wrap_body`, the layout every status body takes) rather than being cut, because cfgd never generalizes what it is telling a person will be installed; `every_operand_list_a_subject_renders_names_every_operand` walks the rendered string of each shape at every budget 80..=200 — and a route's package is an operand: `ManagerAction::Provision` held the `DeclaredProvision` a module's `aliases: {apt: rustc}` produced and rendered `provision cargo via apt` over an `apt-get install rustc`, so all four readers promised work no part of the run performed. The one suppression is an operand the sentence already holds (a route whose package IS the manager's name). `every_manager_action_subject_names_every_operand_it_holds` binds each variant's fields with no `..`, so a new variant — or a new field on an existing one — is classified before the file compiles. `lanes::node_subject` / `lanes::lane_occupant` / `lanes::blocker_subject` are the ONE narrowing of it: a wait line's reason and a swept dependent's error name the blocker by the ROW the reader can see — never by a manager token no row is headed with (`provision npm via apt — waiting on apt` beside a row called `refresh apt index` repeated the muted half's last word and named nothing) and never by that row's operand list, which is on the same screen already: `blocker_subject` takes the occupant's HEAD (`action_display_head` — `brew install`, `deploy`), so `brew install gum — queued behind brew install` points at a row rather than restating eleven package names inside a neighbour's reason. `Prerequisite` narrows to its tool for the same reason, the head of its own row. `every_blocker_is_named_by_the_row_the_reader_can_see` matches every variant exhaustively.
- `script_run_subject` / `module_script_subject` / `hook_script_subject` / `bare_script_subject` (same file) — the partial views for callers holding a script's parts rather than an `&Action`; never rebuild `"{marker}: {body}"` by hand.
- `condense_action_desc_for_display(action, desc)` (same file) — the narrower gate for a raw description string that is not an action subject; never apply it to a value you persist.
- `compose_in_flight_subject(theme, text)` (`output/spinner.rs`) — the ONE composition of an in-flight label, over all five live entry points (`spinner`, `progress_bar`, `set_message`, `output_window[_at]`). It FOLDS the text (`cursor_safe`) and paints it; **it does not EDIT it**. A trailing `…` is part of the subject, not decoration: `condense_script_label`'s truncation arms produce it, so stripping it made the running row read as a broken script (`postApply: if command -v pipx >/dev/null 2>&1; then`) while the settled row that replaces it kept the marker — one action spelled two ways in two adjacent lines. A caller never writes a decorative ellipsis of its own either: `no_in_flight_label_carries_a_trailing_ellipsis` walks every call site in both crates for a literal ending in `…` or `...`, so the rule holds at authoring time rather than by a runtime edit that cannot tell the two apart.
- `system_resource_key(configurator, key)` (same file) — the ONE composition of a system setting's `<configurator>.<key>` identity; three surfaces mint and match it, so a byte of divergence records unresolvable drift.
- `system_key_doubling_error(configurator, key)` (same file) — the ONE statement of the no-self-prefix rule and its diagnostic; never restate the check or the message at a third site.
- `pre_skip_doubling_error(subject, reason)` (same file) — the third member of that family, for a WITHHELD row's two slots. A pre-skipped action renders `<subject> — <reason>`, painted in opposite emphasis on purpose, so a subject naming the very thing the reason says is missing spends both on one noun (`publish 3 vars to the session manager — no session manager`). Judged on the reason minus the `no ` an absence opens on, so `NO_SESSION_MANAGER` stays the ONE wording of that absence — read by the plan, the apply's skip detail and `status`'s session row — and the doubling is fixed on the SUBJECT side (`publish 3 vars to the live session`, naming what the work is FOR). `no_pre_skip_reason_repeats_a_noun_its_subject_already_names` walks every action `Action::pre_skip_reason` answers `Some` for and asserts the table's completeness against that function's own source, so a second gate of that shape is classified before it can ship a doubled row.
- `compliance::snapshot_content_hash(snapshot)` — the ONE serialize-and-digest for a compliance snapshot, dropping the volatile timestamp so an unchanged machine hashes the same twice.
- `CFGD_BACKUP_SUFFIX` / `cfgd_backup_path(target, extra)` / `backup_file(target)` (`reconciler/sidecar.rs`) — the ONE spelling and ONE writer of the sidecar cfgd leaves beside a target it is about to displace, with `SidecarOutcome::detail()` the ONE wording. Three displacers: the adoption path, and `backup restore`'s and `backup rollback`'s pre-write safety copy (both through `backup/restore.rs`'s `take_safety_copy`), which is a sidecar beside the SOURCE and never a snapshot of the unit — not in its destination, its count, its retention or its `backup_runs` ledger. Never compose the name, the copy, or the sentence at a call site: `backup restore`'s `Previous contents …` hint reads the outcome's `reused` bit through that same `detail()`, so a restore over bytes an earlier copy already holds says `already backed up at` rather than claiming a write it did not make (`safetyCopyReused` in `-o json`). `every_sidecar_report_is_worded_by_sidecar_outcome_detail` walks every production function in both crates that holds a sidecar for a literal supplying a verb of its own. **One copy is retained per target**, enforced where the sidecar is WRITTEN (`prune_stamped_sidecars`, called from every settle point of `backup_file`) so the adoption path and the restore keep one invariant rather than two: the reservation's own stamped shapes (`<base>.<stamp>` / `<base>.<stamp>-<n>`, judged by `is_stamped_sidecar_name`) are pruned and nothing else beside the target is touched. The primary `<target>.cfgd-backup` is never a prune candidate — it holds what predated cfgd, which `profile update` and module removal restore from. `backup::rollback_copy(unit)` reads the newest surviving copy (by mtime, a stamped name winning a tie) and `rollback_backup` / `report_rollback` put it back through the restore's own envelope, safety copy included: the contents a rollback displaces may be work written AFTER the restore, which no snapshot and no other sidecar holds, so the one verb whose purpose is reversibility must not be the one write that cannot be undone. Order is what makes that safe — the payload is STAGED into a temp dir first, so the prune the new safety copy triggers cannot reach the copy being put back. A directory sidecar recreates the symlinks inside the tree (`copy_dir_recursive_preserving_symlinks`, taken by the sidecar writer and by `stage_payload`, and matched by `overlay_dir`'s own link arm) where a SNAPSHOT still skips them: an overlay adds and replaces but never deletes, so a link the copy dropped is one the put-back can never restore and the source lands as neither generation. `is_stamped_sidecar_name` is read by BOTH directions: a name cfgd never minted is neither pruned nor published over live data, or cfgd would protect `<base>.mine` from deletion and then write its contents over the operator's source.
- `Reconciler::backing_up(targets)` — the targets a conflict settled as `Backup`, copied aside as the displacing action EXECUTES; both file-writing paths route through `back_up_adopted_target`.
- `is_unmanaged_file` / `sweep_unmanaged_file_targets` / `apply_conflict_policy` / `sweep_label` / `ResolvedConflict` / `UNMANAGED_SKIP_REASON` / `unmanaged_conflict_error` (`reconciler/adopt.rs`) — the ONE classification of "does this target hold a file cfgd never wrote", and the ONE non-prompting sweep. The CLI keeps only the PROMPT.
- `mark_unmanaged_drift(record, strategy, config_dir, state)` + `UNMANAGED_DRIFT_CAUSE` (same file) + `FileDriftResult.unmanaged` — the READ-side half: a drifted finding on a target cfgd never wrote is a different problem with a different fix. All four producers mark.
- `effective::effective_file_strategies(profile, modules, config_dir, default)` — where a producer holding only a target looks its RESOLVED strategy up; three consumers each applying their own `unwrap_or(default)` is how the sweep and `diff` disagreed.
- `PushOutcome` / `PackOutcome` + `oci::artifact_row_detail(digest, platform)` (`cfgd-core/src/oci/`) — what a push RETURNS and how its settled row words it: the manifest digest, and the platform the push resolved and stamped as `OCI_ANNOTATION_PLATFORM`. A caller never re-derives the platform and never serializes the `--platform` flag in its place — a `module push` with no flag emitted `"platform": null` for an artifact whose manifest, and whose Module `PLATFORMS` column, said `linux/amd64`. Nor does the block carry a header kv row for it: `resolve_platform` is the flag or this host, so a `Platform` row keyed on the flag could only ever restate the parenthetical — the same fact twice in one five-line block, and never able to differ. The detail states it unconditionally, so `module push` and `image pack` read the same whether or not a flag named it; `image pack`'s `Base` keeps its row precisely because nothing else reports it, and `module build`'s `Targets` because there the repetition is structural (one occurrence per rendering LEVEL). `no_artifact_verb_serializes_its_platform_flag_as_the_platform_it_resolved` walks the three artifact verbs on both halves — the payload key filled from the flag, and a header key whose value the settled row's detail already prints.
- `oci::artifact_platforms(reference)` (`cfgd-core/src/oci/pull.rs`) — the platforms an already-pushed artifact declares, read off its manifest alone (no blob downloaded). Reads both shapes `push_module` and `push_module_multiplatform` write; an artifact declaring neither answers an empty list, never an error. The operator reaches it through `ArtifactPlatformReader`, never directly, so a controller test names no registry.
- `status::drift_section(doc, drift, checked_live, verified, scan_note, row)` (`crates/cfgd/src/cli/status.rs`) — the ONE frame of an empty Drift section on both `status` surfaces, and the ONE place its role is chosen: `Role::Ok` only when a check stands behind the verdict (`verified` = this run scanned, or a scan is on record — `last_scan_at.is_some()` on the fleet view, `checked_live` alone on the per-module view, which holds no stamp), `Role::Info` otherwise, the role its sibling `No applies recorded yet` takes for the same kind of absence. A green tick over `never scanned` claimed a verdict nothing had produced, while `diff` refuses one for a check that could not run (`Drift undetermined`, `Role::Warn`, `build_diff_doc`). The two are one rule — neither paints `Ok` over an unverified fact — and `no_recorded_verdict_claims_a_check_that_never_ran` pins both surfaces and `diff` together.
- `SIGNATURE_VERIFIED` / `SIGNATURE_UNVERIFIED` / `SIGNATURE_UNSIGNED` / `SIGNATURE_UNKNOWN` (`cfgd-crd/src/lib.rs`) — the ONE four-word vocabulary a person reads for a module's signature, written ONLY by the controller that ran the check. The `SIGNATURE` printer column binds to `status.signature`; `kubectl cfgd status` reads that same field (`MODULE_SIGNATURE_POINTER`) and spells its absence `unknown` — a Module no reconcile has written is a check that has not run, whatever its spec declares; the one exception is a raw `status.verified: true` beside no word (an operator predating the word), which is the single fact the bool asserts alone. Never derive a NEGATIVE word from `status.verified` and the spec: the bool pair cannot express `unknown`, and deriving `unverified` from a declared key reported cosign rejecting an artifact nothing had looked at. `status.verified` stays on the wire as the raw bool. `a_status_less_module_reads_unknown_on_the_plugin_and_the_crd_column_alike` pins the read; `every_kind_with_conditions_exposes_its_readiness_condition_as_a_column` pins that every CRD writing conditions carries its readiness one (`Available` for a Module) as a `kubectl get` column.
- `oci::check_signature(reference, opts)` → `SignatureCheck` (`Valid` / `Rejected` / `Undetermined`) (`cfgd-core/src/oci/sign/mod.rs`) — `verify_signature` as a three-way verdict, for every caller that DISPLAYS or records the outcome. Turning a bare `Err` into "unverified" claims cosign rejected the artifact, which a missing cosign or an unreachable registry does not support; `verify_signature` itself stays right where any failure is fatal.
- `OciReference::uses_plain_http()` (`cfgd-core/src/oci/mod.rs`) — whether cfgd reaches this registry over HTTP (loopback, or named in `OCI_INSECURE_REGISTRIES`). `api_base` and cosign both read it: cosign opens its own connection and only treats loopback as plain HTTP unaided, so `oci/sign`'s `apply_registry_scheme` passes `--allow-insecure-registry` on every cosign subcommand. `every_cosign_subcommand_this_module_spells_declares_the_registry_scheme` walks the population.
- `ArtifactFactsReader` + `ArtifactVerifier` + `RegistryBackoff` (`cfgd-operator/src/controllers/mod.rs`) — the two seams a Module reconcile reaches a registry through, and the memo that keeps a failed visit from being repeated on the next watch event. Every component that READS a registry (operator, CSI driver, agent) also carries an `extraEnv` in `chart/cfgd`; `every_registry_reading_component_exposes_the_same_registry_knob` walks the chart.
- `Reconciler::recording_scope(scope)` — what the run's `applies` row records in place of the profile name, for a `--module` run with no profile to name. The stored column stays spelled `profile`; `cli::status::derivable_profile` is the ONE read of it.

- `explain::LevelWidths::of(fields)` + `field_row` / `push_tree_rows` (`crates/cfgd/src/cli/explain/mod.rs`) — the three column widths a field level's rows pad to. A MARK the legend tells the reader to scan for can only be scanned in a COLUMN: `[+]` concatenated onto a variable-width type span landed at six different x positions down eight rows, and ` (required)` — which sits between the type and the mark — moved every mark after it again. Measured over the LEVEL, the same unit the name column is measured over (in the `--recursive` tree that is one parent's children, so two sibling groups at the same indent legitimately pad differently); a level with nothing required spends no width on the flag, so a list without one is byte-identical. The last filled column's padding is trimmed, `command_list` padding every key to the widest anyway. `every_field_row_mark_lands_in_a_column` walks both marks on both surfaces, per level.
- **A union-typed field in `explain` renders its shapes once and its fields by name** (`schema::union_type_join` / `union_variants`, `explain::own_fields` / `row_type_span` / `is_expandable`, `FieldNode::is_variant`) — four rules, one walk. A type span names what the schema named: a `$ref` member of a union renders its `$defs` name (`<([]string | BrewSpec)>`, on the heading, the Fields column, the Variants row and `-o json`'s `type`), and `object` is reserved for a member that genuinely has none (`apk`'s inline arm). A Variants row carries ONE fact: its name IS its shape (`BrewSpec`, `[]string`), so it renders no `<type>` span — `[]string  <[]string>` spent two columns on one fact. A `Variants` section discloses SHAPES; it never replaces the `Fields` list: a union whose one object arm is the only shape with fields lists that arm's keys where a plain object would have listed its own (`own_fields`), `resolve_field_path` already drilling through the arm. A `[+]` marks a row a reader can NAME in a path; a shape's name is a rendered type, never a path segment, so a variant row never earns the mark — the legend's `<field>` once substituted to `brew.object`, which nothing resolves. `every_union_typed_field_renders_its_shapes_once_and_its_fields_by_name` walks every union-typed field of every schema and renders its page; `explain_union_drilldown_human` pins `profile.spec.packages.brew` byte for byte.
- **No `explain` description names a POSITION** (`crates/cfgd/src/cli/explain/mod.rs`) — the renderer sorts every level alphabetically (`sorted_by_name`), so a doc comment written against DECLARATION order describes a layout the reader is not looking at: `BrewSpec::file`'s `instead of (or alongside) the lists below` rendered UNDER the `casks` row it claimed to precede, and three sibling references (`CargoSpec::file`, `ModulePackageEntry::prefer`, the CRD `PackageEntry::name`) were correct only by luck of the alphabet. A field names its SIBLINGS instead, and the ordering stays the renderer's business alone. `no_explain_description_names_a_position_on_the_screen` walks every field of every kind `explain` can render, at every depth (`all_schemas`, so a CRD spec and the hand-authored TeamConfig tree are covered too), and flags a positional word only where it CLOSES its phrase — `a version below this` compares two values and names no row.

## Error wording

- `CfgdError`'s variants (`errors/mod.rs`) — a top-level variant carries a `<noun> error:` label only when the label is LOAD-BEARING for some member of its inner enum. A sentence that already names its own subject reads perfectly without one, and on an action row — which wears its own ✗ and its own subject — the label is a third layer saying nothing: `✗ provision cargo via apt — package error: cargo bootstrap failed: cargo still not available after bootstrap` said cargo three times and "bootstrap" twice about one failure. `CfgdError::kind()` is what a `-o json` consumer routes a domain on, so nothing structured depends on the prose label. Transparent today: `File`, `Package`, `Secret`, `Source`, `Composition`, `Module`, `Skill`, `Backup`. Labelled, each because one member names no subject of its own: `Config` (`key '{key}' not found`), `State` (`migration failed`), `Daemon` (`service error` / `watch error`), `Upgrade` (`download failed`), `Generate` (`validation failed`), `Oci` (`archive error` / `build error`), `Io` (`std::io::Error` names nothing at all). The verdict is a judgment, not a predicate a scan computes, so `no_error_a_row_renders_opens_on_a_category_label` holds the table AND checks the source against it: a new variant fails the test until it is classified, with the reason a labelled one keeps its label.
- `PackageError::BootstrapFailed` — the one variant whose sentence its CALLERS build, so it carries no label of its own (`#[error("{message}")]`) and every construction site's message names its own manager or the package it was after. See the `PackageExec` row above for how a provision's verification words itself.

## Shell quoting (generated shell files)

Quoting is per-dialect; there is no one correct escaper. Every `*_quoted` helper returns the COMPLETE token **including its quotes** — that is what makes a bare interpolation into `alias {}={}` impossible to write by accident.

- `posix_double_quoted(value)` — a complete `"…"` token for bash/zsh.
- `escape_double_quoted(s)` — the BODY of the above, for a caller joining escaped fragments inside one pair of quotes. A plain `$NAME` / `${NAME}` still expands; `$(cmd)`, `$((…))` and every `${x…}` form become literal.
- `posix_single_quoted(value)` — a complete `'…'` token for POSIX sh, and the correct quoting for a systemd `environment.d` assignment.
- `fish_single_quoted(value)` — a complete `'…'` token for fish.
- `powershell_single_quoted(value)` — a complete `'…'` token; fully literal, and the default choice for PowerShell.
- `powershell_double_quoted(value)` / `escape_powershell_double_quoted(s)` — the interpolating pair, for a declared value carrying `$env:` references.
- `cmd_double_quoted(value)` — a complete `"…"` token for `cmd.exe`/batch, doubling `%`.

A PowerShell function-wrapper alias carries its command as a quoted string built into a script block at CALL time (`function n { & ([scriptblock]::Create('<cmd> @args')) @args }`) — pasted between the braces, a `}` closes the function early.

## Filesystem

- `default_config_dir()` — cross-platform config dir.
- `expand_tilde(path)` — expand `~/` to home (`HOME`, then `USERPROFILE` on Windows).
- `fold_home_in_text(text)` — the ONE DISPLAY inverse of `expand_tilde` (a local `fold_home_to_tilde` in `cli/plan_ops.rs` shipped beside it and was folded in): every `<home>/` in a rendered subject reads `~/`, so one report cannot spell `$HOME` two ways (`write ~/.cfgd.env` two rows above `deploy /home/tj/.config/nvim/init.lua`). The rule is for every DISPLAY slot of one report, not only the action subject: a header kv VALUE naming a path under home folds too (`config_profile_rows`'s `Config`, `ApplyRun::header`'s `Source`, every directory `cfgd paths` lists), and so do `init`'s `Cloned … into` / `Skipped clone into` rows and the `source ~/.cfgd.env` hint, so the row a reader copies and the row above it spell one directory one way. Applied at the subject seams — `action_display_subject_within` and `backup::restore_subject` — those three header producers, `cli/init/source.rs`'s two clone rows, and every table CELL or row that renders a RECORDED path (`status`'s Managed Resources and Drift rows, `status <module>`'s Deployed Files and Drift rows, `source show`'s `URL` and Managed Resources, `source remove`'s table, `source list`'s `Source` column, `backup list`'s `Source` column, `module show`'s `Directory`, `diff`'s per-target rows); never to a STORED or SERIALIZED resource id, a `format_plan_item` subject or anything `-o json` carries, all of which keep the absolute path a script can `cat` — a cell rendering an id is a display slot and folds, the payload beside it keeping the string. Never a `tracing!` line either: a journal is read by scripts and other hosts, and as another user `~` is ambiguous there, so the daemon's `health endpoint at /home/tj/…/cfgd.sock` banner stays absolute beside the folded `Config` row and carries `// native-ok: journal line, not a display slot` (`path-handling.md`, "When native IS correct"); `no_journal_line_folds_the_home_directory` walks both crates for a fold reaching a `tracing::` argument. `cfgd status` folded its `Config` row and spelled `/home/tj/.cfgd.env` six lines below it, so the fix's own report named `$HOME` two ways. `a_report_wide_enough_for_its_wait_reasons_keeps_its_one_column` renders header AND tree and asserts no `<home>/` reaches the screen while `format_plan_item` keeps it; `a_restore_subject_folds_the_home_directory` pins the backup seam; `no_report_slot_spells_the_home_directory_absolutely` renders the three header producers and every listing above under a test home, failing a `<home>/` in the human text and a payload without it.
- `normalize_path_entry(entry, home)` — fold ONE `PATH` entry to the form two spellings of the same directory compare equal in (`$HOME`/`${HOME}`/`~` resolved, separators folded, trailing `/` dropped). COMPARISON only; never render the result. Every env dialect's derived manager-PATH line drops a directory the declared `PATH` already carries through this one key.
- `PATH_LIST_SEPARATOR` / `is_inherited_path_ref(segment)` — the host's `PATH` list separator (`;` on Windows, `:` elsewhere) and the ONE predicate for a segment that REFERS to the ambient `PATH` (`$PATH`, `${PATH}`, `$env:PATH`, `%PATH%`) rather than naming a directory. Both are read by `fold_env_layer` and by the env engine's own `PATH` line, so the fold and the render cannot disagree about where the inherited value sits.
- `absolutize_path(path)` — make a path absolute LEXICALLY without requiring it to exist; use at any CLI entry point. Never canonicalizes, so a symlinked config keeps the name the user gave it.
- `resolve_relative_path(path, base)` — resolve relative to base with traversal validation.
- `resolve_managed_file_source(source, config_dir)` — the ONE resolution of a `spec.files[].source` against the config dir, taken by BOTH readers of that field.
- `validate_path_within(path, root)` — canonicalize and verify containment.
- `validate_no_traversal(path)` — reject a reference containing `..` or naming nothing of its own; use for any path cfgd reads or writes.
- `validate_plain_name(raw)` — stricter, judged on the RAW string. Use for any string that NAMES something cfgd creates under a root it may later delete or mount wholesale; Windows shapes are rejected on every host, so a name is valid everywhere or nowhere.
- `atomic_write(target, content)` / `atomic_write_str` — atomic temp+rename write returning the SHA256; use instead of `fs::write` in ALL production code. Replaces a symlink at the target rather than following it.
- `atomic_write_merged(target, content)` — the `strategy: Patch` write: resolve a symlink first, so the target keeps its mode and its link identity.
- `atomic_write_resolved[_str](target, content)` — the FOLLOW-the-symlink variants, for a user-owned file cfgd did not author where a stow/chezmoi link must survive. A dangling link is written at the link path itself.
- `ensure_parent_dir(target)` — create a file's parent; use instead of the inline `if let Some(parent)` idiom. For a named directory, call `create_dir_all` directly.
- `write_scaffold(kind, path, body)` (`crates/cfgd/src/cli/helpers.rs`) — scaffold writes in the binary crate: the modeline pinned to the BINARY's version, plus an atomic write. Rewrites of user-owned files must not use it.
- `rewrite_user_yaml(path, &value)` (same file) — rewrites of user-owned YAML: re-prepends the leading comment block, prunes absent sections and undeclared scalar defaults. Use instead of raw `to_string` + `atomic_write_str`; it is why no field carries `skip_serializing_if`.
- `quoted_assignment(name, value)` (same file) — the ONE rendering of a declared env var or alias as `name="value"`, for every surface that shows one (the `Set env` / `Set alias` confirmations, `status <module> --show-values`). Quoted UNCONDITIONALLY through `posix_double_quoted`, the same quoter the generated env file's `export`/`alias` lines are written with, so the line reporting a write and the file it wrote spell one assignment one way; a value holding a space read unquoted is a different value (`catn=cat -n` reads as the alias `cat`). NOT for a module-review surface (`module registry`'s pre-approval listing), which must show the declared bytes rather than an escaped form, and not for the `Removed env` / `Removed alias` halves, which take a bare key. `every_assignment_a_setter_confirms_renders_through_the_one_quoter` walks the population.
- `copy_dir_recursive(src, dst)` — recursive tree copy; correct ONLY where cfgd owns the destination.
- `carry_dir_mode(src, dst)` — best-effort directory mode copy; call it AFTER populating `dst`.
- `create_symlink(source, target)` — cross-platform; Windows picks the reparse type by resolving a RELATIVE target against the LINK's own parent (`symlink_dir_probe`), never the process CWD, and refuses a host that holds no privilege through `symlink_error` — the ONE Developer Mode wording, which every preserving copy (`copy_dir_recursive_preserving_symlinks` and `move_dir`'s cross-filesystem fallback, both through one `recreate_link`) wraps with the link's path inside the SOURCE tree rather than degrading to the skip. `mklink /J` needs no privilege and reads back as a symlink, so a stock Windows host can hold links it cannot recreate, and a dropped one is the neither-generation loss the preserving copy exists to prevent.
- `is_same_inode(a, b)` — same file, two paths, one moment.
- `file_identity(path)` — that identity as ONE value, captured now and compared later by a holder that must notice its path being re-pointed. `None` reads as "cannot say", never "different".
- `try_file_identity(path)` — the same probe REPORTING why it failed; reach for it wherever "not the file I opened" and "I could not look" must lead to different actions.
- `file_permissions_mode(metadata)` / `set_file_permissions(path, mode)` / `is_executable(path, metadata)` — Unix mode bits and exec-bit; no-ops or extension checks on Windows.
- `capture_file_state(path)` / `capture_file_resolved_state(path)` / `FileState` — content, hash, permissions and symlink state, unfollowed and followed.

## Process / commands

The **30-second memo convention** and the exclusion a TTL guard needs are in `util/process.rs`'s and `test_helpers.rs`'s module docs. Read them before adding a memo or a pin.

- `command_available(cmd)` — is this command on PATH; the `is_some()` view over `command_path`.
- `command_path(cmd)` — resolve a command to its executable path, memoized per name (misses included). Searches `$PATH` then the bootstrapped directories.
- `command_resolution_generation()` / `invalidate_command_resolution()` — the ONE counter behind the path and availability memos. Any new path that puts a binary on the machine, or takes one off it, calls the invalidator.
- `ProviderRegistry::package_managers()` / `system_configurators()` + `add_*` / `extend_*` / `set_package_managers` (`providers/mod.rs`) — the read and write halves of two PRIVATE vectors; every mutator retires its own availability sweep. Never widen the fields back to `pub`.
- `PackageContext::installed_for(manager)` (`providers/installed.rs`) — the ONE question "what does this manager report installed". Never call `installed_packages[_with_versions]` from a caller holding a context; a command reading twice threads ONE context.
- `ProviderRegistry::manager_map()` — the ONE `name → &dyn PackageManager` map every module-resolution entry point takes; keyed by REGISTERED name, never the family.
- `PackageManagerExt::available_version_memoized(package)` (`providers/available.rs`) — the ONE question "what does this manager currently OFFER". Never call the trait's `available_version` from a resolution or display path.
- `SecretCache` (`providers/secret_cache.rs`) — the ONE secret resolution per `(backend, reference)` per RUN. Not process-scoped and no TTL: it holds plaintext, so its lifetime is the unit of work.
- `Reconciler::fill_planned_versions(modules, managers)` (`reconciler/plan.rs`) — prices a plan's SURVIVING packages; **every caller of `Reconciler::plan` fills through it on the same reconciler**, the description being persisted as well as rendered.
- `modules::resolve_package(entry, module, platform, managers, installed)` — the ONE site that decides which manager a module package lands on, and the ONE producer of a `ResolvedPackage`. A bare entry (no `prefer`) is satisfied by the available manager that ALREADY HOLDS it (`installed`, the run's `PackageContext`; the platform default is asked first, then the others by name, `deny` and `aliases` respected), and only a package nobody holds falls to the platform default — `- name: npm` means "npm on this machine", and resolving it to apt beside a brew-held npm planned six hundred debs on the daemon's first tick. An authored `prefer` is honoured over a holder. Every planning path threads its context (`RunContext::package_context()` in the CLI, which lends the invocation's ONE `InstalledEnumerations`; `PackageContext::new` in `init`, `module create --apply` and the daemon); `None` keeps the default for a surface with no state to read. The in-run twin is `Reconciler::package_survives_elision(mgr, installed, pkg, provisioned)` — the ONE elision predicate, whose `provisioned` arm elides a tool THIS run's own cascade delivered after resolution had already read the listings. `a_bare_entry_resolves_to_the_available_manager_that_already_holds_it`, `an_authored_prefer_list_outranks_the_manager_that_holds_the_package` and `a_bare_entry_another_manager_holds_is_neither_re_resolved_nor_planned` pin the rule; `every_resolved_package_producer_routes_through_the_one_resolver` walks both crates for a second producer.
- `modules::fill_available_versions(packages, managers)` — the unconditional per-package form for the two surfaces that print a version without planning (`doctor`, `module show`); no planning path may call it.
- `command_output_with_timeout(cmd, timeout)` — run a `Command` with a timeout, killing on overrun. It OWNS the stdio configuration; never set stdio yourself.
- `terminate_process(pid)` — SIGTERM / TerminateProcess.
- `exit_status_reason(status)` — the ONE rendering of why a child ended; never `status.code().unwrap_or(-1)`, which names an impossible code while hiding the signal that explains it.
- `stdout_lossy_trimmed(output)` / `stderr_lossy_trimmed(output)` — trimmed lossy-UTF8 capture.
- `output::renderer::wrap::wrap_body(body, prefix, cols)` / `wrap_body_with_trailer(...)` (`output/renderer/wrap.rs`) — the ONE layout of a message body into physical rows: every continuation hangs under the first word after the leading glyph, measured ONCE off the first logical line, and a logical line's OWN leading indent stacks on that hang for the rows it wraps onto. Passing the hang as both prefixes lost the indent: a caveat's wrapped sub-item slid left under the bullet and read as a new item of the outer list. `wrap_body_with_trailer` wraps through `wrap_body`, so it cannot disagree. `a_wrapped_line_keeps_its_own_indent_on_its_continuation_rows` pins it at a narrow width.
- `output::captured_output_detail(msg)` — the ONE fold from a message carrying a CHILD's captured output into a rendered DETAIL slot: bounded at `window::VISIBLE_LINES` (the same tail the live window showed while the command ran), elided from the MIDDLE so the head that names the failure and the tail that diagnoses it both survive, and kept as LINES because `compose_status` already lays a detail's continuations out. `collapse_to_subject_line` is its one-physical-row twin and takes the same bound — a slot that must be one row cannot become forty screens of `Downloaded <crate>`. Both are DISPLAY-only: `journal_fail`, `ActionResult.error` and the `-o json` payload keep the full text. A package manager's stderr reaches either of them only through `packages::shared::command_failure_reason`; `command_failure_reason_is_the_only_place_a_managers_stderr_becomes_a_message` walks `packages/` and hatches a PARSE with its reason.
- `packages::shared::hand_child_bootstrapped_path(cmd)` + `run_pkg_query(manager, cmd)` / `pkg_run` / `run_pkg_cmd*` (`crates/cfgd/src/packages/shared/mod.rs`) — the ONE handoff of the directories this run bootstrapped to a manager child, and the wrappers every spawn of a manager binary under `packages/` takes. `available_version` prices through `run_pkg_query` (which also bounds the wait at `PKG_CMD_TIMEOUT`), and the two probes that spawn on their own (`tool_version_from`, `pip_python_version`) call the handoff first; fifteen pricing arms and both probes once spawned bare, so a brew this run had just bootstrapped answered available and then priced nothing, its `--version` shelling out to a `ruby` the inherited PATH could not name. A spawn of something that is NOT a manager (`stat`, `useradd`) carries `// own-path-ok: <why>` on the line or the line above; a builder that sets its own PATH (`brew_cmd`) is left alone by the handoff itself. `every_manager_spawn_under_packages_inherits_the_bootstrapped_dirs` walks the crate; `a_version_probe_reaches_a_sibling_the_manager_shim_finds_through_the_bootstrapped_dirs` pins the behaviour under an empty PATH.
- `is_root()` — euid==0 / `IsUserAnAdmin()`.
- `hostname_string()` — system hostname; `"unknown"` on failure.
- `tracing_env_filter(default)` — `EnvFilter` from the environment with a fallback.
- `env_or(var, default)` — read an env var with a fallback; the ONE spelling for the two server binaries. A read that must WARN on a bad value belongs beside the operator's own parsers.
- `await_shutdown_request()` — the ONE SIGINT+SIGTERM registration-and-select for a server binary (`#[cfg(unix)]`); a caller adds logging and never its own handler. `daemon::ShutdownSignals` is deliberately separate.
- `output::claim_termination_signals()` — tells the live region's cursor-restore hook that THIS process handles SIGINT/SIGTERM itself, so the hook restores the cursor and leaves the exit to the owner instead of emulating the default handler. Called first by every cooperative registration (`ShutdownSignals::install`, `await_shutdown_request`, `apply`'s abort handlers, the MCP listener); a new signal owner calls it before registering.
- `require_tool(name, install_hint)` — the uniform "X not found" error for every `command_available`-gated flow.
- `tool_cmd(env_var, default)` — the generic seam-honouring `Command` factory.
- `systemctl_cmd()` / `systemctl_available()` / `SYSTEMCTL_BIN_ENV` — the ONE `systemctl` factory, predicate and seam. Never `Command::new("systemctl")` (unshimmable and unbounded) or `command_available("systemctl")` (answers from PATH while the spawn answers from the seam).
- `session_manager_available()` / `NO_SESSION_MANAGER` — whether THIS host has a live-session environment manager (`setx` / `launchctl` / `systemctl --user`), and the ONE wording for its absence. The plan, the apply's skip detail and `status`'s session row all answer from it, so no two surfaces can disagree about whether the publish can happen.
- `reg_cmd()` / `REG_BIN_ENV` — the same for the Windows registry, shared by the session-env refresh and the `windowsRegistry` configurator. The registry is not a path, so redirecting the BINARY is a test's only sandbox.
- Keyed system configurators name their own seams beside their `tool_cmd` factories (`CFGD_GSETTINGS_BIN`, `CFGD_XFCONF_QUERY_BIN`, `CFGD_KREADCONFIG_BIN` / `CFGD_KWRITECONFIG_BIN`, `CFGD_DEFAULTS_BIN`; `windowsRegistry` reuses `CFGD_REG_BIN`). They gate availability differently — seam-answered for `gsettings`/`xfconf`/`kdeConfig`, platform-gated for `macosDefaults`/`windowsRegistry`. Drive them through `test_helpers::ToolShim`.
- `register_bootstrapped_path_dirs(dirs)` — make the PATH directories cfgd created THIS RUN visible to later resolutions; never `set_var("PATH", …)`, unsound once any thread is live.
- `bootstrapped_path_dirs()` — a snapshot of that registry.
- `path_with_dirs_prepended(current, dirs)` — the ONE composition of a PATH whose leading entries are `dirs`; `None` when the value would not change, so a caller leaves the environment alone.
- `process_path_with_dirs_prepended(dirs)` — the same over THIS process's PATH, and what every consumer outside cfgd-core calls, so the read takes the same guard every production reader does.
- `restore_bootstrapped_path_dirs(dirs)` — test-only rewind; reach for it through `BootstrappedPathDirsGuard`.

## Git

- `git_cmd_safe(url, ssh_policy)` — a `Command` for git with `GIT_TERMINAL_PROMPT=0` and configurable host-key checking; required for anything that may touch a remote.
- `git_cmd_local()` — the LOCAL-only factory (`config`, `tag -v`, `add`, `commit`, `rev-parse`, `log`). Use instead of `Command::new("git")` for every local invocation.
- `try_git_cmd(url, args, label, ssh_policy)` — run via `git_cmd_safe`, `true` on success; the CLI-first fallback before every git2 network operation, preventing SSH hangs.
- `resolve_repo_reference(value)` — the ONE resolution of a user-written repository reference, and what EVERY user-facing entry point calls: `acme/config` is both a shorthand and a relative path, and only the filesystem can say which was meant.
- `expand_github_shorthand(value)` — the ONE `owner/repo` → GitHub URL expansion, answered from the STRING alone; reach for `resolve_repo_reference` in any command path.
- `detect_default_branch(repo_dir)` — best-effort `origin/HEAD` then local `HEAD`.
- `detect_git_remote()` / `detect_git_head()` — the CWD repo's origin URL and HEAD SHA; use for artifact provenance instead of re-deriving.
- `git_ssh_credentials(url, username, allowed)` — the git2 credential callback (SSH agent + HTTPS helper).
- `fetch_git_source(git_src, cache_base, module_name, printer)` (`modules/git.rs`) — the ONE materialization of a module's git source, and the only thing deciding whether a transfer is needed. Its two short-circuits must never be re-derived at a call site.
- `is_git_source(value)` (`modules/git.rs`) — the ONE git-URL predicate, pure and scheme-based, deliberately never probing the filesystem. `is_clonable_source` layers `--from`'s extra arms rather than widening it.

## Sigstore / cosign

- `cosign_cmd()` — the ONE cosign factory; consumers add the subcommand and flags. Use instead of `Command::new("cosign")` anywhere Sigstore work is shelled out.
- `oci::COSIGN_PREDICATE_TYPES` + `oci::attestation_type_name(uri)` — the ONE fold between the WIRE vocabulary (the `predicateType` a manifest annotation records) and the FLAG vocabulary (`cosign verify-attestation --type`). Every surface naming an attestation type reads the right column; an unknown predicate is reported verbatim, which is also what `--type` takes for it.
- `oci::artifact_facts(reference)` → `ArtifactFacts { platforms, attestations }` — the ONE registry visit answering both "what platforms does this artifact declare" and "what attestations hang off it" (the cosign `.att` tag). One read, so a status can never mix two visits; the subject manifest is the fallible half, and an unreadable `.att` reads as no attestations.

## Hashing / versions

- `sha256_hex(data)` / `sha256_digest(data)` — SHA256 as hex, and as an OCI-style `sha256:<hex>`; use instead of inline `Sha256::digest`.
- `strip_sha256_prefix(s)` — strip `sha256:`; idempotent.
- `short_commit(commit)` — the 12-char display form of a commit id; every human surface naming a commit (`source show`, `sync`, the daemon sync log) renders through it, persisted/`-o json` ids stay full. It owns the FORM only: `init::source::commit_detail(commit)` owns the SPELLING a PROSE slot gives it (`at <commit>`), and a row naming a checkout reads both, never a bare id. A commit in a named table COLUMN (`sources_table`'s `Commit`) or a kv KEY (`source show`'s `Last Commit`) takes neither — the header is its noun. `every_checkout_row_spells_its_revision_through_the_one_derivation` walks `cli/init/` for a `checkout_facts` revision reaching a detail any other way.
- `init::source::checkout_detail(dir)` (`crates/cfgd/src/cli/init/source.rs`) — the detail slot of every `init` row naming a config directory this run did not create: the checkout's own origin and `short_commit` revision, read off the repository rather than echoed from the caller's `--from`. `init` MINTS the checkout every later surface reports on and runs before there is any state to read the answer back out of, so a row naming only the directory cfgd itself chose carried no fact the reader could not have predicted — and on the already-populated arms that directory may hold a different remote entirely. `None` for a directory that is no checkout, so a scaffolded config's row is unchanged. `every_checkout_creating_verb_names_its_source_and_revision` drives all three arms.
- `Sha256Stream` — the incremental form (`update`, `absorb_file`, `finish_hex` / `finish_digest`), for a digest over many inputs one of which is a file. The seam order IS the digest: never reorder a caller's parts.
- `parse_loose_version(s)` — 1/2/3-part version → semver `Version`.
- `version_satisfies(version, requirement)` — semver range check.

## Locks / reconcile

- `acquire_apply_lock(state_dir)` — the exclusive apply lock; returns an RAII guard.
- `acquire_source_lock(cache_dir, on_wait)` — the source-cache mutex, and the one lock that BLOCKS rather than refusing. Never the apply lock; `on_wait` announces through `printer.alert`, never `status_simple`. **Nothing in cfgd ever deletes a lock file.**
- `sources::discard_cached_checkout(cache_dir, name, printer)` — the ONE deletion of a source's cached checkout, holding the source lock; never a bare `remove_dir_all`.
- `resolve_effective_reconcile(module, profile_chain, config)` / `EffectiveReconcile` — per-module reconcile settings resolved from patches, with no `Option`s left.

## Config inputs (what a derivation READ)

- `record_config_input(path)` — the ONE report that a file or listing was consulted while deriving typed config, called from the READ sites so a caller never GUESSES the file set.
- `ConfigInputRecorder::start()` / `.finish()` — the RAII frame; frames nest, so an inner recorder does not steal an outer one's entries.
- `ConfigInputs::unchanged()` — re-stat every entry. An EMPTY set answers `false`: a derivation that recorded nothing has nothing that could report it stale.
- `daemon::tick_cache::TickCache` — the daemon's holder built on the above, and the shape a second long-lived holder reaches for rather than minting a new fingerprint scheme.

## Encryption

- `is_file_encrypted(path, backend)` — sops (`sops.mac` + `lastmodified`) or age header detection.

## Snapshot normalizers

Plain `cfgd_core::*` exports from `util/paths.rs` that make a captured render host-stable. Snapshot tests reach them through `normalize_for_snapshot`; call one directly only for a single fold.

- `normalize_for_snapshot(captured, &[(path, label)])` — the composed entry point: `\`→`/`, CRLF→LF, and each path substituted with its label.
- `normalize_cfgd_version(s, version)` — substitute the EXACT running version, so a wrong version still fails to match.
- `normalize_snapshot_durations(raw)` — replace every ` (N.Ns)` elapsed suffix with ` (XXs)`; never re-implement the scan, or two suites disagree about what counts as timing.

## Test guards

Reached via `cfgd_core::test_helpers::*`, gated behind the `test-helpers` Cargo feature. Pair every env-var consumer with `serial_test::serial`; which exclusion each TTL guard needs is in that module's doc.

- `BootstrappedPathDirsGuard::capture()` / `::capture_and_clear()` — RAII snapshot+restore of the bootstrapped-PATH registry, REQUIRED in any fixture driving a bootstrap. Emptying `PATH` is not sufficient for a "not found" branch; use `capture_and_clear`.
- `path_env_read_guard()` / `path_env_mutation_guard()` — the gate over the process-global `PATH`. A mutating test takes the WRITE guard, declared before its `EnvVarGuard`; a test asserting a SUCCESSFUL resolution takes the read guard. Never spawn while holding the write guard.
- `await_queued_path_writer(timeout)` — blocks until a writer is queued; the observable a concurrency test needs instead of a sleep.
- `await_blocking_source_acquire(timeout)` — the same observable for the source lock's blocking arm. Wait on this, never on `on_wait`, which fires BEFORE the acquire.
- `CommandPathMemoTtlGuard::{never_expires, always_expired, pinned}` — RAII pin of the `command_path` TTL; needs no serialization, its users asserting on an ANSWER a pin cannot change.
- `AvailableVersionMemoTtlGuard::…` — the same for the available-version ceiling; pair with `#[serial_test::serial(available_version_memo)]`. Two fixtures claiming one manager offers two versions of one package are claiming two machines — make them agree.
- `AvailabilityMemoTtlGuard::…` — the same for the provider-availability sweep; pair with the UNNAMED `#[serial_test::serial]`, the group its own count tests share.
- `ConfigReuseMaxAgeGuard::…` / `ModuleReuseTtlGuard::…` — the tick cache's two reuse ceilings; two guards rather than one so a test can say which it is asserting about. Pair with `#[serial_test::serial(tick_cache_reuse)]`.
- `GitRefreshWindowGuard::…` — the module git-cache refresh window. The pin SERIALIZES ITSELF, so no call site can forget an attribute; it excludes pins and only pins.
- `measured_in_a_stable_generation(measure)` — run `measure` in a window where nothing else moved the resolution generation. REQUIRED by every memo-hit claim; the closure must be re-runnable, so build its subject INSIDE it.
- `captured_text(&buf)` — the ONE read of a capture buffer, ANSI-stripped. Use it for any assertion about TEXT even though captures pin colour off: a negative `!contains` goes VACUOUS the moment styling is on.
- `Printer::for_test_split_streams(verbosity)` — the split-stream capture, the one constructor that can state a stdout-purity claim directly.
- `Printer::for_test_with_theme_colored(theme, verbosity)` — the ONE capture whose buffer carries ANSI, for a test whose subject IS the escapes. To assert the colour DECISION instead, call `output::printer::colors_must_be_disabled(&format)` and render nothing.
- **The three live-region capture constructors.** They differ only in where indicatif draws, so the wrong one answers a different question SILENTLY rather than failing:
  - `Printer::for_test_live_scrollback()` — indicatif draws to a HIDDEN target, so the buffer is only what was committed permanently. For commit order and commit-exactly-once; blind to the region by construction.
  - `Printer::for_test_with_live_bars()` — indicatif draws into the SAME buffer, so it holds every paint in order. For interleaving, garbling and draw ORDER; it cannot answer "how many lines are on screen".
  - `Printer::for_test_live_terminal(rows, cols)` — indicatif draws onto an EMULATED SCREEN, cursor moves and clears executed. The ONLY surface that sees a line the region drew and never erased; it reads the VISIBLE screen, so a taller region loses evidence off the top. Also the ONLY capture that answers `wrap_columns()` with a width: every golden and split-stream capture answers `None`, under which no column ever retreats and no subject is ever cut, so a claim about a WIDTH (a column surviving, a wait reason complete) is pinned here at the width it is about, never inferred from an unbounded render.
- `spinner::start_spinner_animation(bar)` (`output/spinner.rs`) — the ONE way a bar starts animating, and **the LAST call of any spinner setup**. A steady tick redraws from a background thread against whatever the bar holds at the instant it is enabled, and the animated template is `{spinner} {msg}`, so a tick started before the message is in paints a lone glyph on an empty line — a status row naming no work. Both starters call it (`build_spinner`, `LiveRow::window`); its `debug_assert` on a non-empty message is the ordering seam a test pins. `LiveRow::set_status` holds the same rule from the settle side, disabling the tick before it repaints.
- `test_printer()` — a bare Quiet `Printer` for a fixture that asserts nothing about output. NEVER `Printer::new`, which inherits the invoking terminal and hangs on an unanswered prompt under a pty.
- `EnvVarGuard::set(key, value)` / `::unset(key)` — RAII env-var save/restore, restored even on panic.
- `with_test_env_var(var, value, f)` — the scoped-closure form.
- `spawn_blocking_with_test_home(f)` — `spawn_blocking` re-installing the caller's test-home thread-local; REQUIRED for every blocking dispatch whose closure may resolve `~`.
- `ConcurrencyWitness` + `MockPackageManager::with_concurrency_witness(w)` — proof that two lanes really overlapped, so a concurrency test asserts a peak, never a wall-clock bound.
- `ProbePath::containing(&[names])` — a `PATH` of one temp dir holding exactly the named executables (Unix-only). Assert the negative under an empty `PATH` and the positive here, one binary at a time.
- `CosignTestShim::install()` / `::builder()...install()` — the fake-cosign shim (argv logging, keygen mode, exit code, canned stderr), restoring the prior seam on drop.
- `freeze_last_scan_at(&StateStore, timestamp)` — pins the recorded scan stamp and then REFUSES every later write, so a `cfgd`-crate test can drive the refused-write branch. The refusal lives in the database FILE, the consumer opening its own store.

## Upgrade

- `cleanup_old_binary()` (`upgrade.rs`) — remove the `.exe.old` left by the Windows rename-dance self-upgrade; no-op on Unix, called from `main.rs` on startup.

## What NOT to do

- Don't create new utility files outside `cfgd-core/src/util/`. Shared functions go in the existing topic file that matches the helper's domain.
- Don't add the same helper as a sibling of an existing topic file. Pick the existing topic.
- Don't create a brand-new topic file unless the helper genuinely doesn't fit any existing one — three string-validation functions don't justify a new file when `strings.rs` exists.
- Don't duplicate a function that already exists. Search this catalog first.
- Don't create local timestamp/hash/command-check wrappers — use the shared ones above.
- Don't restate an item's rationale here. This file says WHAT and WHEN; the item's rustdoc says WHY, and a second copy drifts.
